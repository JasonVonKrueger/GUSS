<?xml version="1.0" encoding="UTF-8"?><unload unload_date="2026-09-21 19:51:07">
<sys_remote_update_set action="INSERT_OR_UPDATE">
<application display_value="Global">global</application>
<application_name>Global</application_name>
<application_scope>global</application_scope>
<application_version/>
<collisions/>
<commit_date/>
<deleted/>
<description>*) added search feature
*) various UI updates</description>
<inserted/>
<name>GlobalUpdateStudio-v2.2</name>
<origin_sys_id/>
<parent display_value=""/>
<release_date/>
<remote_base_update_set display_value=""/>
<remote_parent_id/>
<remote_sys_id>079dac5c8393c31466b576d6feaad363</remote_sys_id>
<state>loaded</state>
<summary/>
<sys_class_name>sys_remote_update_set</sys_class_name>
<sys_created_by>nervoussow</sys_created_by>
<sys_created_on>2026-09-21 19:51:06</sys_created_on>
<sys_id>90892690832f4bd466b576d6feaad3a8</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_updated_by>nervoussow</sys_updated_by>
<sys_updated_on>2026-09-21 19:51:06</sys_updated_on>
<update_set display_value=""/>
<update_source display_value=""/>
<updated/>
</sys_remote_update_set>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sys_ui_action_63fd64dc8393c31466b576d6feaad369</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sys_ui_action"&gt;&lt;sys_ui_action action="INSERT_OR_UPDATE"&gt;&lt;action_name&gt;open_guss_tab&lt;/action_name&gt;&lt;active&gt;true&lt;/active&gt;&lt;client&gt;true&lt;/client&gt;&lt;client_script_v2&gt;&lt;![CDATA[function onClick(g_form) {

}]]&gt;&lt;/client_script_v2&gt;&lt;comments/&gt;&lt;condition&gt;current.canWrite() &amp;amp;&amp;amp; current.sys_id == (new GlideUpdateSet()).get() &amp;amp;&amp;amp; current.state == 'in progress'&lt;/condition&gt;&lt;form_action&gt;true&lt;/form_action&gt;&lt;form_button&gt;true&lt;/form_button&gt;&lt;form_button_v2&gt;false&lt;/form_button_v2&gt;&lt;form_context_menu&gt;true&lt;/form_context_menu&gt;&lt;form_link&gt;true&lt;/form_link&gt;&lt;form_menu_button_v2&gt;false&lt;/form_menu_button_v2&gt;&lt;form_style/&gt;&lt;format_for_configurable_workspace&gt;false&lt;/format_for_configurable_workspace&gt;&lt;hint/&gt;&lt;isolate_script&gt;true&lt;/isolate_script&gt;&lt;list_action&gt;false&lt;/list_action&gt;&lt;list_banner_button&gt;false&lt;/list_banner_button&gt;&lt;list_button&gt;false&lt;/list_button&gt;&lt;list_choice&gt;false&lt;/list_choice&gt;&lt;list_context_menu&gt;false&lt;/list_context_menu&gt;&lt;list_link&gt;false&lt;/list_link&gt;&lt;list_save_with_form_button&gt;false&lt;/list_save_with_form_button&gt;&lt;list_style/&gt;&lt;messages/&gt;&lt;name&gt;Open in GUSS&lt;/name&gt;&lt;onclick&gt;openGussTab()&lt;/onclick&gt;&lt;order&gt;100&lt;/order&gt;&lt;script&gt;&lt;![CDATA[function openGussTab() {
    var updateSetId = g_form.getUniqueValue();
    var targetUrl = '/GUSS.do?sys_id=' + updateSetId;

    // Use ServiceNow's native g_navigation API
    if (typeof g_navigation !== 'undefined') {
        g_navigation.open(targetUrl, '_blank');
    } else {
        // Fallback to top window context if g_navigation is unavailable
        top.window.open(targetUrl, '_blank');
    }
}]]&gt;&lt;/script&gt;&lt;show_insert&gt;false&lt;/show_insert&gt;&lt;show_multiple_update&gt;false&lt;/show_multiple_update&gt;&lt;show_query&gt;false&lt;/show_query&gt;&lt;show_update&gt;true&lt;/show_update&gt;&lt;sys_class_name&gt;sys_ui_action&lt;/sys_class_name&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-09-09 00:52:44&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;63fd64dc8393c31466b576d6feaad369&lt;/sys_id&gt;&lt;sys_mod_count&gt;5&lt;/sys_mod_count&gt;&lt;sys_name&gt;Open in GUSS&lt;/sys_name&gt;&lt;sys_overrides/&gt;&lt;sys_package display_value="Global" source="global"&gt;global&lt;/sys_package&gt;&lt;sys_policy/&gt;&lt;sys_scope display_value="Global"&gt;global&lt;/sys_scope&gt;&lt;sys_update_name&gt;sys_ui_action_63fd64dc8393c31466b576d6feaad369&lt;/sys_update_name&gt;&lt;sys_updated_by&gt;nervoussow&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-18 13:55:10&lt;/sys_updated_on&gt;&lt;table&gt;sys_update_set&lt;/table&gt;&lt;ui11_compatible&gt;true&lt;/ui11_compatible&gt;&lt;ui16_compatible&gt;false&lt;/ui16_compatible&gt;&lt;/sys_ui_action&gt;&lt;/record_update&gt;</payload>
<payload_hash>629583482</payload_hash>
<remote_update_set display_value="GlobalUpdateStudio-v2.2">90892690832f4bd466b576d6feaad3a8</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>nervoussow</sys_created_by>
<sys_created_on>2026-09-21 19:51:06</sys_created_on>
<sys_id>54892690832f4bd466b576d6feaad3a9</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a0b4ccb7860000001</sys_recorded_at>
<sys_updated_by>nervoussow</sys_updated_by>
<sys_updated_on>2026-09-21 19:51:06</sys_updated_on>
<table>sys_update_set</table>
<target_name>Open in GUSS</target_name>
<type>UI Action</type>
<update_domain>global</update_domain>
<update_guid>0d4b399f74130f94f76411d013fbb4e2</update_guid>
<update_guid_history>0d4b399f74130f94f76411d013fbb4e2:629583482,953b759fa6130f94490812f188f08987:456082653,ccdd30e0465b87141c255c80c0691b10:629583482,230edb5094974714842244b9e7029ffe:-252113799,a48d9fdcf257471445ef5f11e2504bb4:1459098027,cce1bcd820d3c314253c8e86adbd6b9c:-589041755</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sys_ui_page_6ec0bbb583a2cf9066b576d6feaad3be</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update table="sys_ui_page"&gt;&lt;sys_ui_page action="INSERT_OR_UPDATE"&gt;&lt;category&gt;general&lt;/category&gt;&lt;client_script&gt;&lt;![CDATA[// ── Server-provided context ─────────────────────────────────────────────────
// The HTML field renders a JSON data island (#guss-context-data) via Jelly so
// the server can hand off the current update set, session token, and the
// create-artifacts property. Everything else lives here in Client Script.
(function() {
  var contextEl = document.getElementById('guss-context-data');
  var parsed = {};
  if (contextEl) {
    try {
      parsed = JSON.parse(contextEl.textContent || contextEl.innerText || '{}');
    } 
	catch(e) {
      parsed = {};
    }
  }

  window.GUSS_CONTEXT = {
    updateSetId:         parsed.updateSetId || '',
    userToken:           parsed.userToken || '',
    createArtifactsJson: parsed.createArtifactsJson || ''
  };
})();

(function() {
  // ── DOM references ──────────────────────────────────────────────────────────
  const appEl             = document.getElementById('guss-app');
  const treeEl            = document.getElementById('guss-tree');
  const metaEl            = document.getElementById('guss-meta');
  const noticeEl          = document.getElementById('guss-notice');
  const reloadBtn         = document.getElementById('guss-reload');
  const tabsEl            = document.getElementById('guss-tabs');
  const viewerEl          = document.getElementById('guss-viewer');
  const workspaceEl       = document.querySelector('.guss-workspace');
  const sidebarResizerEl  = document.getElementById('guss-sidebar-resizer');

  // ── Application state ───────────────────────────────────────────────────────
  let treeData                 = [];   // Parsed artifact tree nodes
  let openTabs                 = [];   // Tabs currently open in the viewer
  let activeTabKey             = '';   // Key of the currently visible tab
  let currentUpdateSetId       = '';   // sys_id of the update set actually loaded into the tree
  let currentUpdateSetName     = '';
  let currentUpdateSetState    = '';
  let currentContextUpdateSetId   = '';   // Update set that was active on load
  // Separate from the above: what the landing-page dropdown has picked, for
  // the "Make current set" action. These can legitimately differ from the
  // loaded tree's update set (that's the whole point of the dropdown), so
  // they're tracked separately rather than reusing currentUpdateSetId - a
  // previous version of this code reused it for both, which meant an empty
  // dropdown (e.g. no in-progress global update sets) blanked out the
  // already-loaded tree's update set info as a side effect.
  let selectedLandingUpdateSetId    = '';
  let selectedLandingUpdateSetName  = '';
  let selectedLandingUpdateSetState = '';
  let isLoadingEmptyState      = true; // True while initial data fetch is running
  let availableUpdateSets      = [];   // All update sets fetched from server

  // ── "Add New File" artifact menu config ────────────────────────────────────
  // Loaded from sys_properties; falls back to hardcoded defaults.
  const CREATE_ARTIFACTS_PROPERTY_NAME = 'global_update_set_studio.create_artifacts';
  const DEFAULT_CREATE_ARTIFACTS = [
    { label: 'Business Rule',  icon: '\u2699\ufe0f',   tableName: 'sys_script' },
    { label: 'Script Include', icon: '\ud83d\udce6',   tableName: 'sys_script_include' },
    { label: 'Client Script',  icon: '\ud83d\udda5\ufe0f', tableName: 'sys_script_client' },
    { label: 'UI Policy',      icon: '\ud83d\udccb',   tableName: 'sys_ui_policy' },
    { label: 'UI Action',      icon: '\u25b6\ufe0f',   tableName: 'sys_ui_action' },
    { label: 'Scheduled Job',  icon: '\u23f0',         tableName: 'sysauto_script' },
    { label: 'Fix Script',     icon: '\ud83d\udd27',   tableName: 'sys_script_fix' },
    { label: 'UI Page',        icon: '\ud83d\udcc4',   tableName: 'sys_ui_page' },
    { label: 'REST Message',   icon: '\ud83c\udf10',   tableName: 'sys_rest_message' },
    { label: 'Table',          icon: '\ud83d\uddc3\ufe0f', tableName: 'sys_db_object' }
  ];
  let createArtifacts = DEFAULT_CREATE_ARTIFACTS.slice();

  // ── Sidebar resize constants ────────────────────────────────────────────────
  const SIDEBAR_DEFAULT_WIDTH = 330;
  // Keeps enough room in the sticky header for the title plus the add/open/
  // search button row (~142px) without the title being squeezed to nothing.
  const SIDEBAR_MIN_WIDTH     = SIDEBAR_DEFAULT_WIDTH - 30;
  const SIDEBAR_MAX_WIDTH     = SIDEBAR_DEFAULT_WIDTH + 50;

  // ── Utility: inline notice bar above the tree ───────────────────────────────
  function setNotice(text) {
    if (!text) {
      noticeEl.style.display = 'none';
      noticeEl.textContent = '';
      return;
    }
    noticeEl.style.display = 'block';
    noticeEl.textContent = text;
  }

  // ── Sidebar resize: clamp and apply width via CSS custom property ───────────
  function setSidebarWidth(widthPx) {
    if (!appEl) return;
    const bounded = Math.max(SIDEBAR_MIN_WIDTH, Math.min(SIDEBAR_MAX_WIDTH, widthPx));
    appEl.style.setProperty('--guss-sidebar-width', bounded + 'px');
  }

  // Wire the drag handle so the sidebar can be resized left/right
  function initSidebarResizer() {
    if (!sidebarResizerEl || !appEl) return;

    let dragging  = false;
    let startX    = 0;
    let startWidth = SIDEBAR_DEFAULT_WIDTH;

    function onMouseMove(event) {
      if (!dragging) return;
      setSidebarWidth(startWidth + (event.clientX - startX));
    }

    function onMouseUp() {
      if (!dragging) return;
      dragging = false;
      document.body.style.cursor = '';
      document.body.style.userSelect = '';
      document.removeEventListener('mousemove', onMouseMove);
      document.removeEventListener('mouseup', onMouseUp);
    }

    sidebarResizerEl.addEventListener('mousedown', function(event) {
      event.preventDefault();
      dragging = true;
      startX = event.clientX;
      const rawWidth = getComputedStyle(appEl).getPropertyValue('--guss-sidebar-width').trim();
      const parsed = parseInt(rawWidth || String(SIDEBAR_DEFAULT_WIDTH), 10);
      startWidth = Number.isFinite(parsed) ? parsed : SIDEBAR_DEFAULT_WIDTH;
      document.body.style.cursor = 'ew-resize';
      document.body.style.userSelect = 'none';
      document.addEventListener('mousemove', onMouseMove);
      document.addEventListener('mouseup', onMouseUp);
    });
  }

  // ── Utility: HTML-escape a value for safe interpolation into innerHTML ───────
  function esc(value) {
    return String(value || '').replace(/[&amp;&lt;&gt;"']/g, function(ch) {
      return ({ '&amp;': '&amp;amp;', '&lt;': '&amp;lt;', '&gt;': '&amp;gt;', '"': '&amp;quot;', "'": '&amp;#39;' })[ch];
    });
  }

  // Returns a folder or file icon span for a tree node
  function iconFor(node) {
    return node.kind === 'folder'
      ? '&lt;span class="guss-folder-icon"&gt;\ud83d\udcc1&lt;/span&gt;'
      : '&lt;span class="guss-file-icon"&gt;\ud83d\udcc4&lt;/span&gt;';
  }

  // Remove the .guss-selected highlight from all tree rows
  function clearSelected() {
    document.querySelectorAll('.guss-node-row.guss-selected').forEach(function(el) {
      el.classList.remove('guss-selected');
    });
  }

  // ── Record URL helpers ──────────────────────────────────────────────────────

  // A real ServiceNow table name: starts with a letter, then letters/digits/
  // underscores only (covers core "sys_*", custom "u_*", and scoped "x_*"
  // tables). tableName ends up in a URL path segment (iframe src / link
  // href), so anything that doesn't match this is rejected rather than
  // trusted verbatim.
  const SAFE_TABLE_NAME_PATTERN = /^[a-zA-Z][a-zA-Z0-9_]{0,79}$/;

  function isSafeTableName(tableName) {
    return SAFE_TABLE_NAME_PATTERN.test(String(tableName || ''));
  }

  // Split a sys_update_xml.name (e.g. "sys_script_abc123...") into table + sys_id.
  // The name field comes from update-set data, which can originate from an
  // imported/applied XML file rather than this instance, so the table portion
  // is validated the same as a hand-typed value would be.
  function parseTargetFromName(nameValue) {
    const raw = String(nameValue || '').trim();
    const idx = raw.lastIndexOf('_');
    if (idx &lt;= 0 || idx &gt;= raw.length - 1) return { tableName: '', targetSysId: '' };
    const suffix = raw.substring(idx + 1);
    if (!/^[0-9a-fA-F]{32}$/.test(suffix)) return { tableName: '', targetSysId: '' };
    const tableName = raw.substring(0, idx);
    if (!isSafeTableName(tableName)) return { tableName: '', targetSysId: '' };
    return { tableName: tableName, targetSysId: suffix };
  }

  // URL to open an existing record in a frameless view
  function buildRecordUrl(tableName, targetSysId) {
    if (!isSafeTableName(tableName) || !targetSysId) return '';
    return '/' + tableName + '.do?sys_id=' + encodeURIComponent(targetSysId) + '&amp;sysparm_stack=no';
  }

  // URL to open a new (blank) record form
  function buildNewRecordUrl(tableName) {
    if (!isSafeTableName(tableName)) return '';
    return '/' + tableName + '.do?sys_id=-1&amp;sysparm_stack=no';
  }

  // ── "Add New File" tab creation ─────────────────────────────────────────────

  // Open a tab for creating a brand-new artifact of the chosen type
  function openCreateArtifactTab(item) {
    const key = 'new:' + item.tableName;
    const existing = openTabs.find(function(t) { return t.key === key; });

    if (!existing) {
      openTabs.push({ key: key, label: item.label, url: buildNewRecordUrl(item.tableName) });
    }

    activeTabKey = key;
    renderTabs();
  }

  // Bridge for the standalone "Add New File" modal IIFE, mirroring
  // GUSS_OPEN_RECORD_TAB below.
  window.GUSS_OPEN_NEW_ARTIFACT_TAB = function(item) {
    if (!item || !item.tableName) return;
    openCreateArtifactTab({
      tableName: String(item.tableName),
      label: String(item.label || item.tableName)
    });
  };

  // ── Application / update-set helpers ───────────────────────────────────────

  // Normalise an app key for case-insensitive comparison ("Global" vs "global")

  // Show/hide the "Make current set" button depending on whether the selected
  // update set differs from the context that was active on page load
  function setMakeCurrentButtonVisibility() {
    const makeCurrentBtn = document.getElementById('guss-empty-make-current');
    if (!makeCurrentBtn) return;

    if (!currentContextUpdateSetId) {
      makeCurrentBtn.style.display = 'none';
      return;
    }

    const isCurrentSelection = selectedLandingUpdateSetId === currentContextUpdateSetId;
    makeCurrentBtn.style.display = isCurrentSelection ? 'none' : 'inline-block';
  }

  // ── Empty / landing state controls ─────────────────────────────────────────
  // Wires up the Update Set dropdown rendered in the empty state view (no tabs open).
  function renderEmptyStateControls() {
    const updateSetSelectEl   = document.getElementById('guss-empty-update-set');
    const stateEl             = document.getElementById('guss-empty-state-value');
    if (!updateSetSelectEl) return;

    // Populate the update-set &lt;select&gt; and pre-select the current update set
    function populateUpdateSetSelect(preferredUpdateSetId) {
      updateSetSelectEl.innerHTML = '';

      if (!availableUpdateSets.length) {
        const option = document.createElement('option');
        option.value = '';
        option.textContent = 'No update sets available';
        updateSetSelectEl.appendChild(option);
        updateSetSelectEl.disabled = true;
        selectedLandingUpdateSetId    = '';
        selectedLandingUpdateSetName  = '';
        selectedLandingUpdateSetState = '';
        if (stateEl) stateEl.textContent = 'Unknown';
        setMakeCurrentButtonVisibility();
        return;
      }

      updateSetSelectEl.disabled = false;
      availableUpdateSets.forEach(function(updateSet) {
        const option = document.createElement('option');
        option.value = updateSet.sysId;
        option.textContent = updateSet.name;
        updateSetSelectEl.appendChild(option);
      });

      // Keep preferred selection if it exists in the list
      let selectedId = preferredUpdateSetId;
      if (!selectedId || !availableUpdateSets.some(function(updateSet) { return updateSet.sysId === selectedId; })) {
        selectedId = availableUpdateSets[0].sysId;
      }
      updateSetSelectEl.value = selectedId;

      const selectedUpdateSet = availableUpdateSets.find(function(updateSet) { return updateSet.sysId === selectedId; }) || availableUpdateSets[0];
      selectedLandingUpdateSetId    = selectedUpdateSet.sysId;
      selectedLandingUpdateSetName  = selectedUpdateSet.name;
      selectedLandingUpdateSetState = selectedUpdateSet.state || 'Unknown';
      if (stateEl) stateEl.textContent = selectedLandingUpdateSetState || 'Unknown';
      setMakeCurrentButtonVisibility();
    }

    // Wire up the update-set select
    updateSetSelectEl.addEventListener('change', function() {
      const selectedUpdateSet = availableUpdateSets.find(function(updateSet) {
        return updateSet.sysId === updateSetSelectEl.value;
      });

      if (!selectedUpdateSet) return;

      selectedLandingUpdateSetId    = selectedUpdateSet.sysId;
      selectedLandingUpdateSetName  = selectedUpdateSet.name;
      selectedLandingUpdateSetState = selectedUpdateSet.state || 'Unknown';
      if (stateEl) stateEl.textContent = selectedLandingUpdateSetState || 'Unknown';
      setMakeCurrentButtonVisibility();
    });

    populateUpdateSetSelect(currentUpdateSetId);
    setMakeCurrentButtonVisibility();
  }

  // An artifact tab's iframe points at the classic record form. There's no
  // direct "record was saved" signal available, but a *second* load event on
  // that same iframe (its first load was just opening the form) means the
  // user navigated inside it - most commonly by saving, which redirects back
  // to itself or to the record list. Treat that as a cue to quietly refresh
  // the sidebar tree/search index in the background, without touching the
  // open tabs. This is a heuristic, not a precise "did it save" detector -
  // other in-form navigation (e.g. opening a related list) also triggers it,
  // which just means an occasional unnecessary refresh rather than stale data
  // silently going unnoticed.
  function watchArtifactIframeForSave(iframeEl) {
    if (!iframeEl) return;
    let hasLoadedOnce = false;
    iframeEl.addEventListener('load', function() {
      if (!hasLoadedOnce) {
        hasLoadedOnce = true;
        return;
      }
      loadTree();
    });
  }

  // ── Tab bar and viewer ──────────────────────────────────────────────────────
  // Renders the tab strip and the active tab's content (iframe or inline HTML).
  // Also handles the empty/loading state when no tabs are open.
  function renderTabs() {
    tabsEl.innerHTML = '';

    if (!openTabs.length) {
      tabsEl.style.display = 'none';
      if (workspaceEl) workspaceEl.style.gridTemplateRows = '1fr';

      // Show spinner while the initial data fetch is still running
      if (isLoadingEmptyState) {
        viewerEl.innerHTML = '&lt;div class="guss-empty guss-loading-wrap"&gt;&lt;div class="guss-loading"&gt;&lt;div class="guss-spinner" aria-hidden="true"&gt;&lt;/div&gt;&lt;div class="guss-loading-text"&gt;Loading update set workspace...&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;';
        return;
      }

      // Landing state: GUSS branding + update-set dropdown
      viewerEl.innerHTML =
        '&lt;div class="guss-empty" style="display:flex; align-items:center; justify-content:center;"&gt;' +
          '&lt;div class="guss-empty-intro"&gt;' +
            '&lt;div class="guss-empty-title"&gt;GUSS&lt;/div&gt;' +
            '&lt;div class="guss-empty-subtitle"&gt;Global Update Set Studio&lt;/div&gt;' +
            '&lt;div class="guss-empty-divider"&gt;&lt;/div&gt;' +
            '&lt;div class="guss-empty-meta"&gt;' +
              '&lt;div class="guss-empty-label" title="Only showing open global update sets"&gt;Update set:&lt;/div&gt;' +
              '&lt;div class="guss-empty-select-wrap"&gt;' +
                '&lt;select id="guss-empty-update-set" class="guss-empty-select" aria-label="Choose update set"&gt;&lt;/select&gt;' +
              '&lt;/div&gt;' +
            '&lt;/div&gt;' +
            '&lt;div class="guss-empty-action-cell"&gt;' +
              '&lt;button id="guss-empty-make-current" class="guss-empty-action" type="button"&gt;Make current set&lt;/button&gt;' +
            '&lt;/div&gt;' +
          '&lt;/div&gt;' +
        '&lt;/div&gt;';
      renderEmptyStateControls();
      return;
    }

    tabsEl.style.display = 'flex';
    if (workspaceEl) workspaceEl.style.gridTemplateRows = '52px 1fr';

    // Build one tab element per open tab
    openTabs.forEach(function(tab) {
      const tabEl = document.createElement('div');
      tabEl.className = 'guss-tab' + (tab.key === activeTabKey ? ' active' : '');
      tabEl.innerHTML =
        '&lt;span class="guss-tab-label" title="' + esc(tab.label) + '"&gt;' + esc(tab.label) + '&lt;/span&gt;' +
        '&lt;span class="guss-tab-close"&gt;\u00d7&lt;/span&gt;';

      tabEl.addEventListener('click', function(event) {
        const closeClicked = event.target &amp;&amp; event.target.classList &amp;&amp; event.target.classList.contains('guss-tab-close');
        if (closeClicked) {
          event.stopPropagation();
          closeTab(tab.key);
          return;
        }
        activeTabKey = tab.key;
        renderTabs();
      });

      tabsEl.appendChild(tabEl);
    });

    // Ensure activeTabKey points at a real tab
    const active = openTabs.find(function(t) { return t.key === activeTabKey; }) || openTabs[0];
    activeTabKey = active.key;

    // Render the active tab's content area
    if (active.url) {
      viewerEl.innerHTML = '&lt;iframe class="guss-iframe" src="' + esc(active.url) + '"&gt;&lt;/iframe&gt;';
      watchArtifactIframeForSave(viewerEl.querySelector('iframe.guss-iframe'));
    }
	else if (active.html) {
      viewerEl.innerHTML = active.html;
    } 
	else {
      viewerEl.innerHTML =
        '&lt;div class="guss-empty"&gt;&lt;div class="guss-panel"&gt;' +
          '&lt;div style="font-weight:600; margin-bottom:10px;"&gt;' + esc(active.label) + '&lt;/div&gt;' +
          '&lt;pre class="guss-muted" style="white-space:pre-wrap; margin:0;"&gt;Unable to derive target table/sys_id from this update XML record.&lt;/pre&gt;' +
        '&lt;/div&gt;&lt;/div&gt;';
    }
  }

  // ── Tab management ──────────────────────────────────────────────────────────

  // Open (or switch to) a tab for an artifact file node
  function openArtifactTab(node) {
    const keyBase = node.tableName &amp;&amp; node.targetSysId
      ? (node.tableName + ':' + node.targetSysId)
      : ('update_xml:' + node.updateXmlSysId);
    const existing = openTabs.find(function(t) { return t.key === keyBase; });
    if (!existing) {
      openTabs.push({ key: keyBase, label: node.name, url: buildRecordUrl(node.tableName, node.targetSysId) });
    }

    activeTabKey = keyBase;
    renderTabs();
  }

  window.GUSS_OPEN_RECORD_TAB = function(record) {
    if (!record || !record.tableName || !record.targetSysId) return;
    openArtifactTab({
      tableName: String(record.tableName),
      targetSysId: String(record.targetSysId),
      name: String(record.name || record.label || record.targetSysId)
    });
  };

  // Open (or refresh) a tab that shows a folder's artifact list
  function openFolderTab(node) {
    const folderTypeName = String(node.typeName || node.name || 'Artifact Type');
    const keyBase = 'folder:' + folderTypeName;
    const existing = openTabs.find(function(t) { return t.key === keyBase; });
    const folderTab = {
      key:   keyBase,
      label: folderTypeName,
      url:   '',
      html:  buildFolderDetailsHtml(node)
    };

    if (existing) {
      // Refresh in place so stale folder data is always updated on re-click
      existing.label = folderTab.label;
      existing.url   = '';
      existing.html  = folderTab.html;
    } 
	else {
      openTabs.push(folderTab);
    }

    activeTabKey = keyBase;
    renderTabs();
  }

  // Close a tab by key; activate the most-recently-opened remaining tab
  function closeTab(tabKey) {
    const idx = openTabs.findIndex(function(t) { return t.key === tabKey; });
    if (idx &lt; 0) return;
    openTabs.splice(idx, 1);
    if (activeTabKey === tabKey) {
      activeTabKey = openTabs.length ? openTabs[openTabs.length - 1].key : '';
    }
    renderTabs();
  }

  // ── Folder detail panel ─────────────────────────────────────────────────────
  // Builds the inline HTML shown when a folder (artifact type) tab is opened
  function buildFolderDetailsHtml(node) {
    const folderTitle = String(node.typeName || node.name || 'Artifact Type');
    const children = Array.isArray(node.children) ? node.children : [];

    const detailsList = children.map(function(child) {
      const action = String(child.action || '').trim();
      let actionLabel = '';
      if (action) {
        const normalizedAction = action.toUpperCase();
        // INSERT_OR_UPDATE is the most common action; show a friendlier label
        actionLabel = normalizedAction === 'INSERT_OR_UPDATE'
          ? ' &lt;em style="color:#475569;"&gt;(New or modified file)&lt;/em&gt;'
          : ' (' + esc(normalizedAction) + ')';
      }
      return '&lt;li style="margin:0 0 10px 0;"&gt;' + esc(child.name || 'Unnamed Artifact') + actionLabel + '&lt;/li&gt;';
    }).join('');

    const detailsBody = detailsList
      ? '&lt;ul style="margin:0; padding-left:22px; color:#334155; font-size:14px; line-height:1.5;"&gt;' + detailsList + '&lt;/ul&gt;'
      : '&lt;div style="color:#64748b; font-size:14px;"&gt;No files found for this type in the current update set.&lt;/div&gt;';

    return '' +
      '&lt;div class="guss-empty" style="padding:30px 36px; background:#ffffff;"&gt;' +
        '&lt;div style="display:flex; align-items:center; gap:12px; color:#0f4f79;"&gt;' +
          '&lt;span style="font-size:28px; line-height:1;"&gt;\ud83d\udcc1&lt;/span&gt;' +
          '&lt;div style="font-size:28px; font-weight:700; line-height:1.1;"&gt;' + esc(folderTitle) + '&lt;/div&gt;' +
        '&lt;/div&gt;' +
        '&lt;div style="margin-top:16px; border:1px solid #bae6fd; background:#f0f9ff; border-radius:14px; padding:20px 24px;"&gt;' +
          '&lt;div style="font-size:16px; font-weight:700; letter-spacing:0.05em; color:#0369a1; margin-bottom:12px;"&gt;DETAILS&lt;/div&gt;' +
          detailsBody +
        '&lt;/div&gt;' +
      '&lt;/div&gt;';
  }

  // ── Tree rendering ──────────────────────────────────────────────────────────

  // Recursively build a &lt;li&gt; element for a single tree node (folder or file)
  function renderNode(node, parentPath) {
    const li = document.createElement('li');
    li.className = 'guss-node';

    const row = document.createElement('div');
    row.className = 'guss-node-row';
    const path = parentPath ? (parentPath + '/' + node.name) : node.name;
    row.dataset.path = path;

    row.innerHTML =
      '&lt;span class="guss-caret"&gt;' + (node.kind === 'folder' ? '\u25b8' : '') + '&lt;/span&gt;' +
      iconFor(node) +
      '&lt;span class="guss-name" title="' + esc(node.name) + '"&gt;' + esc(node.name) + '&lt;/span&gt;';

    li.appendChild(row);

    // Render child nodes inside a collapsible &lt;ul&gt;
    let childrenEl = null;

    if (node.children &amp;&amp; node.children.length) {
      childrenEl = document.createElement('ul');
      childrenEl.className = 'guss-children';
      childrenEl.style.display = 'none';
      node.children.forEach(function(child) {
        childrenEl.appendChild(renderNode(child, path));
      });
      li.appendChild(childrenEl);
    }

    // Toggle open/closed state of a folder node
    function setFolderOpen(isOpen) {
      node.open = !!isOpen;
      if (node.kind === 'folder') {
        const caret = row.querySelector('.guss-caret');
        if (caret) caret.textContent = node.open ? '\u25be' : '\u25b8';
      }

      if (childrenEl) childrenEl.style.display = node.open ? 'block' : 'none';
    }

    if (node.kind === 'folder') setFolderOpen(!!node.open);

    // Clicking the caret toggles the folder without opening the tab
    const caretEl = row.querySelector('.guss-caret');
    if (node.kind === 'folder' &amp;&amp; caretEl) {
      caretEl.addEventListener('click', function(event) {
        event.stopPropagation();
        setFolderOpen(!node.open);
      });
    }

    // Clicking the row opens the appropriate tab (folder detail or artifact record)
    row.addEventListener('click', function(event) {
      event.stopPropagation();
      clearSelected();
      row.classList.add('guss-selected');

      if (node.kind === 'folder') {
        openFolderTab(node);
        return;
      }

      openArtifactTab(node);
    });

    return li;
  }

  // Clear and re-render the entire sidebar tree from treeData
  function renderTree() {
    treeEl.innerHTML = '';
    if (!treeData.length) {
      const empty = document.createElement('li');
      empty.className = 'guss-node';
      empty.innerHTML = '&lt;div class="guss-node-row"&gt;&lt;span class="guss-name guss-muted"&gt;No artifacts found in current update set.&lt;/span&gt;&lt;/div&gt;';
      treeEl.appendChild(empty);
      return;
    }

    treeData.forEach(function(node) {
      treeEl.appendChild(renderNode(node, ''));
    });
  }

  // ── Data fetching ───────────────────────────────────────────────────────────

  // Thin fetch wrapper that attaches the SN user token and parses JSON
  async function fetchJson(url) {
    const headers = { 'Accept': 'application/json', 'Content-Type': 'application/json' };
    if (window.GUSS_CONTEXT.userToken) headers['X-UserToken'] = window.GUSS_CONTEXT.userToken;
    const response = await fetch(url, { method: 'GET', headers: headers, credentials: 'same-origin' });
    if (!response.ok) {
      const text = await response.text();
      throw new Error('HTTP ' + response.status + ' while reading ' + url + ': ' + text);
    }
    return response.json();
  }

  // Parse and validate the JSON array stored in the sys_properties artifact config.
  // Falls back to DEFAULT_CREATE_ARTIFACTS if the value is missing or malformed.
  function normalizeCreateArtifacts(configValue) {
    if (!configValue) return DEFAULT_CREATE_ARTIFACTS.slice();

    let parsed;
    try {
      parsed = JSON.parse(configValue);
    } 
	catch (error) {
      return DEFAULT_CREATE_ARTIFACTS.slice();
    }

    if (!Array.isArray(parsed)) return DEFAULT_CREATE_ARTIFACTS.slice();

    const normalized = parsed
      .map(function(item) {
        if (!item || typeof item !== 'object') return null;
        const label     = String(item.label     || '').trim();
        const tableName = String(item.tableName || '').trim();
        const icon      = String(item.icon      || '').trim();
        if (!label || !tableName) return null;
        return { label: label, tableName: tableName, icon: icon || '\ud83d\udcc4' };
      })
      .filter(function(item) { return !!item; });

    return normalized.length ? normalized : DEFAULT_CREATE_ARTIFACTS.slice();
  }

  // Fetch the artifact menu config from sys_properties
  async function loadCreateArtifactsConfig() {
    const query  = encodeURIComponent('name=' + CREATE_ARTIFACTS_PROPERTY_NAME + '^ORDERBYDESCsys_updated_on');
    const fields = encodeURIComponent('value');
    const url = '/api/now/table/sys_properties?sysparm_display_value=false&amp;sysparm_exclude_reference_link=true&amp;sysparm_query=' + query + '&amp;sysparm_fields=' + fields + '&amp;sysparm_limit=1';

    try {
      const data    = await fetchJson(url);
      const records = Array.isArray(data.result) ? data.result : [];
      const rawValue = records.length ? String(records[0].value || '') : '';
      createArtifacts = normalizeCreateArtifacts(rawValue);
    } 
	catch (error) {
      createArtifacts = DEFAULT_CREATE_ARTIFACTS.slice();
    }

	window.GUSS_CREATE_ARTIFACTS = createArtifacts;
  }

  // Fetch name, state, and application for a single update set
  async function loadUpdateSetInfo(updateSetId) {
    const url = '/api/now/table/sys_update_set/' + encodeURIComponent(updateSetId) + '?sysparm_display_value=false&amp;sysparm_exclude_reference_link=true&amp;sysparm_fields=name,state,application';
    const data = await fetchJson(url);
    const result = data.result || {};
    const applicationRef  = String(result.application || '').trim();
    let applicationName   = applicationRef || 'Global';
    let applicationScope  = 'Unknown';

    // If the application field looks like a sys_id, resolve its display name
    function looksLikeSysId(value) {
      return /^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$/.test(value) ||
             /^[0-9a-fA-F]{32}$/.test(value);
    }

    if (looksLikeSysId(applicationRef)) {
      try {
        const appUrl  = '/api/now/table/sys_scope/' + encodeURIComponent(applicationRef) + '?sysparm_display_value=false&amp;sysparm_exclude_reference_link=true&amp;sysparm_fields=name,scope';
        const appData = await fetchJson(appUrl);
        const appResult = appData.result || {};
        applicationName  = appResult.name  || applicationName;
        applicationScope = appResult.scope || applicationScope;
      } catch (error) {
        applicationName  = applicationName || 'Global';
        applicationScope = 'Unknown';
      }
    }

    return {
      applicationId: applicationRef,
      application:   applicationName,
      scope:         applicationScope,
      updateSetId:   updateSetId,
      name:          result.name  || updateSetId,
      state:         result.state || 'Unknown'
    };
  }

  // Fetch all global-scope applications from sys_scope

  // Row cap for the update-set and artifact list fetches below. There's no
  // pagination past this - a table with more rows than this silently comes
  // back truncated, so both callers report a `truncated` flag rather than
  // just handing back the raw array.
  const ROW_FETCH_LIMIT = 10000;

  // Fetch all update sets (every state) from sys_update_set
  async function loadUpdateSets() {
    const query  = encodeURIComponent('application=global^state=in progress^parent=NULL^base_update_set=NULL^ORDERBYname');
    const fields = encodeURIComponent('sys_id,name,state');
    const url = '/api/now/table/sys_update_set?sysparm_display_value=false&amp;sysparm_exclude_reference_link=true&amp;sysparm_no_count=true&amp;sysparm_query=' + query + '&amp;sysparm_fields=' + fields + '&amp;sysparm_limit=' + ROW_FETCH_LIMIT;
    const data = await fetchJson(url);
    const rawRecords = Array.isArray(data.result) ? data.result : [];
    const updateSets = rawRecords
      .map(function(record) {
        return {
          sysId: String(record.sys_id || '').trim(),
          name:  String(record.name   || '').trim() || 'Unnamed Update Set',
          state: String(record.state  || '').trim() || 'Unknown'
        };
      })
      .filter(function(record) { return record.sysId; })
      .sort(function(a, b) { return a.name.localeCompare(b.name); });

    return { updateSets: updateSets, truncated: rawRecords.length &gt;= ROW_FETCH_LIMIT };
  }

  // Fetch sys_update_xml records for a given update set. Records with
  // action=DELETE represent artifacts removed from the instance, so they are
  // excluded; a type whose updates are all deletions gets no folder at all.
  async function loadArtifacts(updateSetId) {
    const query  = encodeURIComponent('update_set=' + updateSetId + '^action!=DELETE^ORDERBYtype^ORDERBYtarget_name');
    const fields = encodeURIComponent('sys_id,type,target_name,name,action');
    const url = '/api/now/table/sys_update_xml?sysparm_display_value=true&amp;sysparm_exclude_reference_link=true&amp;sysparm_no_count=true&amp;sysparm_limit=' + ROW_FETCH_LIMIT + '&amp;sysparm_query=' + query + '&amp;sysparm_fields=' + fields;
    const data = await fetchJson(url);
    const records = Array.isArray(data.result) ? data.result : [];

    return { records: records, truncated: records.length &gt;= ROW_FETCH_LIMIT };
  }

  // ── Tree data model ─────────────────────────────────────────────────────────

  // Determine a human-readable label for an artifact: prefer target_name, then
  // strip the leading table prefix from the sys_update_xml name
  function normalizeLabel(record) {
    const target = String(record.target_name || '').trim();
    if (target) return target;
    const name = String(record.name || '').trim();
    if (!name) return String(record.sys_id || 'Unnamed Artifact');
    const segments = name.split('_');

    return segments.length &gt; 1 ? segments.slice(1).join('_') : name;
  }

  // Shared with the standalone Add/Open/Search modal IIFEs below, so those
  // don't each carry their own copy of the same logic (which can drift).
  window.GUSS_UTIL = {
    esc: esc,
    parseTargetFromName: parseTargetFromName,
    normalizeLabel: normalizeLabel
  };

  // Flatten the folder-per-type tree into a single list of files for the
  // search modal, tagging each entry with its owning folder's type name.
  function flattenSearchIndex(nodes) {
    const out = [];
    (nodes || []).forEach(function(node) {
      if (node.kind === 'file') {
        out.push({
          name: node.name,
          typeName: '',
          tableName: node.tableName,
          targetSysId: node.targetSysId,
          updateXmlSysId: node.updateXmlSysId
        });
        return;
      }

      if (node.kind === 'folder' &amp;&amp; node.children) {
        node.children.forEach(function(child) {
          out.push({
            name: child.name,
            typeName: node.typeName || node.name,
            tableName: child.tableName,
            targetSysId: child.targetSysId,
            updateXmlSysId: child.updateXmlSysId
          });
        });
      }
    });
    return out;
  }

  // Group raw artifact records into a folder-per-type tree structure
  function buildTree(records) {
    const byType = new Map();
    records.forEach(function(rec) {
      const typeName = String(rec.type || 'Unknown Type').trim() || 'Unknown Type';
      if (!byType.has(typeName)) byType.set(typeName, []);
      const parsed = parseTargetFromName(rec.name || '');
      byType.get(typeName).push({
        kind:          'file',
        name:          normalizeLabel(rec),
        tableName:     parsed.tableName,
        targetSysId:   parsed.targetSysId,
        updateXmlSysId: rec.sys_id || '',
        action:        rec.action || ''
      });
    });

    return Array.from(byType.entries())
      .sort(function(a, b) { return a[0].localeCompare(b[0]); })
      .map(function(entry) {
        const typeName = entry[0];
        const children = entry[1].sort(function(a, b) { return a.name.localeCompare(b.name); });
        return {
          kind:      'folder',
          name:      typeName + ' (' + children.length + ')',
          typeName:  typeName,
          fileCount: children.length,
          open:      false,
          children:  children
        };
      });
  }

  // ── Main load orchestration ─────────────────────────────────────────────────
  // Fetches all data in parallel and rebuilds the tree and meta bar.
  // Called on initial load and whenever the user clicks Reload.
  async function loadTree() {
    try {
      setNotice('');
      isLoadingEmptyState = true;
      if (!openTabs.length) renderTabs(); // Show spinner immediately

      await loadCreateArtifactsConfig();

      const updateSetId = String(window.GUSS_CONTEXT.updateSetId || '').trim();
      if (!updateSetId) {
        metaEl.textContent = 'No current update set selected.';
        currentUpdateSetId    = '';
        currentUpdateSetName  = '';
        currentUpdateSetState = '';
        availableUpdateSets   = [];
        treeData = [];
        window.GUSS_SEARCH_INDEX = [];
        window.GUSS_CURRENT_UPDATE_SET_ID = '';
        renderTree();
        isLoadingEmptyState = false;
        if (!openTabs.length) renderTabs();
        setNotice('Unable to resolve current update set.');
        return;
      }

      metaEl.textContent = 'Loading update set ' + updateSetId + '...';

      // Kick off all three fetches in parallel
      const loaded = await Promise.all([
        loadUpdateSetInfo(updateSetId),
        loadArtifacts(updateSetId),
        loadUpdateSets()
      ]);
      const updateSetInfo    = loaded[0];
      const artifactsResult  = loaded[1];
      const updateSetsResult = loaded[2];
      const artifacts        = artifactsResult.records;

      availableUpdateSets = updateSetsResult.updateSets;

      currentUpdateSetId    = updateSetInfo.updateSetId;
      currentUpdateSetName  = updateSetInfo.name;
      currentUpdateSetState = updateSetInfo.state;

      // Snapshot context so "Make current set" can detect divergence
      currentContextUpdateSetId = currentUpdateSetId;

      treeData = buildTree(artifacts);
      window.GUSS_SEARCH_INDEX = flattenSearchIndex(treeData);
      window.GUSS_CURRENT_UPDATE_SET_ID = currentUpdateSetId;
      renderTree();

      metaEl.innerHTML =
        '&lt;a class="guss-meta-link" href="' + esc(buildRecordUrl('sys_update_set', updateSetInfo.updateSetId)) + '" target="_blank" rel="noopener noreferrer"&gt;' + esc(updateSetInfo.name) + '&lt;/a&gt;' +
        ' \u2022 ' + artifacts.length + ' artifact(s)';
      isLoadingEmptyState = false;
      if (!openTabs.length) renderTabs();

      // Flag it when either fetch hit its row cap, since the tree/dropdown
      // then silently shows a partial list rather than everything that exists.
      const truncationNotices = [];
      if (artifactsResult.truncated) truncationNotices.push('this update set has ' + ROW_FETCH_LIMIT + '+ artifacts - the file list below may be incomplete');
      if (updateSetsResult.truncated) truncationNotices.push('there are ' + ROW_FETCH_LIMIT + '+ open update sets - the update set list may be incomplete');
      if (truncationNotices.length) setNotice('Note: ' + truncationNotices.join('; ') + '.');

    } 
	catch (error) {
      currentUpdateSetId    = '';
      currentUpdateSetName  = '';
      currentUpdateSetState = '';
      availableUpdateSets   = [];
      treeData = [];
      window.GUSS_SEARCH_INDEX = [];
      window.GUSS_CURRENT_UPDATE_SET_ID = '';
      renderTree();
      metaEl.textContent = 'Failed to load update set artifacts';
      isLoadingEmptyState = false;
      if (!openTabs.length) renderTabs();
      setNotice('Failed to load artifacts. Check ACLs for sys_update_set and sys_update_xml.');
    }
  }

  // ── Event listeners and startup ─────────────────────────────────────────────

  function init() {
    if (!appEl || !treeEl || !metaEl || !noticeEl || !tabsEl || !viewerEl || !reloadBtn) return;

    if (reloadBtn) reloadBtn.addEventListener('click', loadTree);
    initSidebarResizer();
    setSidebarWidth(SIDEBAR_DEFAULT_WIDTH);
    renderTabs();
    loadTree();

    // Handle "Make Current Set" button click
    document.addEventListener('click', function(event) {
      if (event.target &amp;&amp; event.target.id === 'guss-empty-make-current') {
        event.preventDefault();
        makeCurrentSet();
      }
    });
  }

  // Make the update set selected in the landing-page dropdown the current
  // set for this user (deliberately the dropdown's selection, not whatever
  // is loaded into the tree - that's the whole point of this action).
  function makeCurrentSet() {
    if (!selectedLandingUpdateSetId) {
      alert('No update set selected');
      return;
    }

    var sessionToken = window.GUSS_CONTEXT &amp;&amp; window.GUSS_CONTEXT.userToken;
    if (!sessionToken) {
      alert('Unable to load the ServiceNow session token.');
      return;
    }

    var body = [
      'sysparm_processor=UpdateSetAjax',
      'sysparm_type=changeUpdateSet',
      'sysparm_value=' + encodeURIComponent(selectedLandingUpdateSetId),
      'sysparm_ck=' + encodeURIComponent(sessionToken)
    ].join('&amp;');

    fetch('/xmlhttp.do', {
      method: 'POST',
      credentials: 'same-origin',
      headers: {
        'Content-Type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'X-UserToken': sessionToken
      },
      body: body
    })
      .then(function(response) {
        if (!response.ok) {
          throw new Error('HTTP ' + response.status);
        }
        return response.text();
      })
      .then(function() {
        var nav = (window.parent &amp;&amp; window.parent.g_navigation) || (window.top &amp;&amp; window.top.g_navigation) || window.g_navigation;
        if (nav &amp;&amp; typeof nav.reloadWindow === 'function') {
          nav.reloadWindow();
        } else {
          window.location.reload();
        }
      })
      .catch(function(error) {
        alert('Failed to set current update set: ' + error.message);
      });
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  }
  else {
    init();
  }
})();

// ── "Add New File" modal ────────────────────────────────────────────────────
// Lists the configured create-artifact types (window.GUSS_CREATE_ARTIFACTS,
// kept up to date by loadCreateArtifactsConfig()) and opens a "new record"
// tab for the chosen type via GUSS_OPEN_NEW_ARTIFACT_TAB.
(function () {
  function byId(id) { return document.getElementById(id); }

  // Shared with the main script IIFE above rather than a private copy.
  var esc = window.GUSS_UTIL.esc;

  function selectItem(item) {
    var opener = window.GUSS_OPEN_NEW_ARTIFACT_TAB;
    if (typeof opener !== 'function') return;
    opener(item);
    hideModal();
  }

  function renderList() {
    var listEl = byId('guss-add-modal-list');
    if (!listEl) return;
    listEl.innerHTML = '';

    var items = Array.isArray(window.GUSS_CREATE_ARTIFACTS) ? window.GUSS_CREATE_ARTIFACTS : [];

    if (!items.length) {
      var empty = document.createElement('div');
      empty.className = 'guss-add-modal-empty';
      empty.textContent = 'No artifact types configured';
      listEl.appendChild(empty);
      return;
    }

    items.forEach(function(item) {
      var button = document.createElement('button');
      button.type = 'button';
      button.className = 'guss-add-modal-item';
      button.innerHTML = '&lt;span class="guss-add-modal-icon"&gt;' + esc(item.icon || ']]&gt;📄&lt;![CDATA[') + '&lt;/span&gt;&lt;span&gt;' + esc(item.label) + '&lt;/span&gt;';
      button.addEventListener('click', function() { selectItem(item); });
      listEl.appendChild(button);
    });
  }

  function showModal() {
    var overlay = byId('guss-add-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'flex';
    renderList();
  }

  function hideModal() {
    var overlay = byId('guss-add-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'none';
  }

  function bindAddButton() {
    var button = byId('guss-add-btn');
    if (!button || button.getAttribute('data-add-modal-bound') === 'true') return;
    button.setAttribute('data-add-modal-bound', 'true');
    button.addEventListener('click', function (evt) {
      evt.preventDefault();
      evt.stopPropagation();
      showModal();
    });
  }

  function bindModalEvents() {
    var closeButton = byId('guss-add-modal-close');
    var overlay = byId('guss-add-modal-overlay');

    if (closeButton &amp;&amp; closeButton.getAttribute('data-close-bound') !== 'true') {
      closeButton.setAttribute('data-close-bound', 'true');
      closeButton.addEventListener('click', hideModal);
    }

    if (overlay &amp;&amp; overlay.getAttribute('data-overlay-bound') !== 'true') {
      overlay.setAttribute('data-overlay-bound', 'true');
      overlay.addEventListener('click', function (evt) {
        if (evt.target === overlay) hideModal();
      });
    }

    if (document.body &amp;&amp; document.body.getAttribute('data-add-modal-esc-bound') !== 'true') {
      document.body.setAttribute('data-add-modal-esc-bound', 'true');
      document.addEventListener('keydown', function (evt) {
        if (evt.key === 'Escape') hideModal();
      });
    }
  }

  // No MutationObserver here: the button and modal overlay are static markup
  // from the page's HTML field, never destroyed/recreated by any other
  // script on this page, so a one-time bind is all that's ever needed.
  function init() {
    bindAddButton();
    bindModalEvents();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  }
  else {
    init();
  }
})();

// ── "Open Artifact" modal ───────────────────────────────────────────────────
(function () {
  function byId(id) { return document.getElementById(id); }

  // Records are cached per table for the lifetime of the page, so re-selecting
  // an artifact type never refetches. Only global-scope records are pulled.
  var recordCache = {};

  var activeTableName = '';   // table currently selected in the modal
  var activeRecords   = [];   // records backing the typeahead for that table
  var selectedRecord  = null; // record chosen from the suggestions list

  var MAX_SUGGESTIONS = 50;
  var FETCH_LIMIT = 1000;

  function parsePropertyValue(raw) {
    if (!raw) return [];

    var parsed = raw;
    if (typeof raw === 'string') {
      try {
        parsed = JSON.parse(raw);
      }
	  catch (e) {
        return [];
      }
    }

    if (!Array.isArray(parsed)) {
      return [];
    }

    var entries = [];
    for (var i = 0; i &lt; parsed.length; i++) {
      var item = parsed[i];
      if (!item || typeof item !== 'object') continue;

      var label = typeof item.label === 'string' ? item.label.trim() : '';
      var tableName = typeof item.tableName === 'string' ? item.tableName.trim() : '';
      if (!label || !tableName) continue;

      var duplicate = false;
      for (var j = 0; j &lt; entries.length; j++) {
        if (entries[j].label === label &amp;&amp; entries[j].tableName === tableName) {
          duplicate = true;
          break;
        }
      }
      if (!duplicate) {
        entries.push({ label: label, tableName: tableName });
      }
    }
    return entries;
  }

  function setHelp(message) {
    var help = byId('guss-open-modal-help');
    if (help) help.textContent = message || '';
  }

  function setRecordGroupVisible(visible) {
    var group = byId('guss-open-record-group');
    if (!group) return;
    group.style.display = visible ? 'block' : 'none';
  }

  function setOpenSubmitVisible(visible) {
    var button = byId('guss-open-modal-submit');
    if (!button) return;
    button.style.display = visible ? 'inline-block' : 'none';
  }

  function hideSuggestions() {
    var suggestions = byId('guss-open-record-suggestions');
    if (!suggestions) return;
    suggestions.innerHTML = '';
    suggestions.style.display = 'none';
  }

  // Clear the typeahead input, suggestions, and any prior selection
  function resetRecordInput(placeholder, visible) {
    selectedRecord = null;
    setOpenSubmitVisible(false);
    var input = byId('guss-open-record-input');
    if (input) {
      input.value = '';
      input.placeholder = placeholder || 'Type to search records...';
      input.disabled = false;
    }
    hideSuggestions();
    setRecordGroupVisible(visible === true);
  }

  function fillSelect(entries) {
    var select = byId('guss-open-modal-select');
    if (!select) return;
    select.innerHTML = '';

    if (!entries.length) {
      var emptyOption = document.createElement('option');
      emptyOption.value = '';
      emptyOption.textContent = 'No artifacts found in property';
      select.appendChild(emptyOption);
      resetRecordInput('No records available', false);
      setHelp('Property parsed, but no valid label/tableName pairs were found.');
      return;
    }

    var first = document.createElement('option');
    first.value = '';
    first.textContent = 'Select a file type';
    select.appendChild(first);

    for (var i = 0; i &lt; entries.length; i++) {
      var option = document.createElement('option');
      option.value = entries[i].tableName;
      option.textContent = entries[i].label;
      select.appendChild(option);
    }
    resetRecordInput('Select a file type first', false);
    setHelp('Loaded ' + entries.length + ' file type' + (entries.length === 1 ? '' : 's') + '.');
  }

  function getSessionToken() {
    var ctx = window.GUSS_CONTEXT || {};
    return ctx.userToken || window.g_ck || '';
  }

  // Render the suggestion list for the current query against activeRecords
  function renderSuggestions(query) {
    var suggestions = byId('guss-open-record-suggestions');
    if (!suggestions) return;

    var trimmed = String(query || '').trim().toLowerCase();
    suggestions.innerHTML = '';

    if (!trimmed) {
      suggestions.style.display = 'none';
      return;
    }

    var matches = [];
    for (var i = 0; i &lt; activeRecords.length &amp;&amp; matches.length &lt; MAX_SUGGESTIONS; i++) {
      if (activeRecords[i].display_value.toLowerCase().indexOf(trimmed) !== -1) {
        matches.push(activeRecords[i]);
      }
    }

    if (!matches.length) {
      suggestions.style.display = 'none';
      return;
    }

    matches.forEach(function(record) {
      var item = document.createElement('div');
      item.className = 'guss-open-suggestion';
      item.setAttribute('role', 'option');
      item.textContent = record.display_value;
      item.addEventListener('click', function() {
        selectedRecord = record;
        var input = byId('guss-open-record-input');
        if (input) input.value = record.display_value;
        hideSuggestions();
        setOpenSubmitVisible(true);
      });
      suggestions.appendChild(item);
    });
    suggestions.style.display = 'block';
  }

  // Point the typeahead at a record list (fresh or cached) for a table
  function applyRecords(tableName, records) {
    activeRecords = records;
    resetRecordInput('Type to search ' + records.length + ' record' + (records.length === 1 ? '' : 's') + '...', true);
    setHelp('Loaded ' + records.length + ' global record' + (records.length === 1 ? '' : 's') + ' from ' + tableName + '.');

    if (!records.length) {
      var input = byId('guss-open-record-input');
      if (input) {
        input.placeholder = 'No global records found';
        input.disabled = true;
      }
    }
  }

  function loadRecordsForTable(tableName) {
    activeTableName = tableName;
    activeRecords = [];

    if (!tableName) {
      resetRecordInput('Select an artifact type first', false);
      return;
    }

    if (recordCache[tableName]) {
      applyRecords(tableName, recordCache[tableName]);
      return;
    }

    resetRecordInput('Loading records...', true);
    var input = byId('guss-open-record-input');
    if (input) input.disabled = true;
    setHelp('');

    var token = getSessionToken();
    var query = encodeURIComponent('sys_scope=global');
    var fields = encodeURIComponent('sys_id,name,sys_name,number,title');
    var url = '/api/now/table/' + encodeURIComponent(tableName) + '?sysparm_limit=' + FETCH_LIMIT + '&amp;sysparm_query=' + query + '&amp;sysparm_fields=' + fields + '&amp;sysparm_exclude_reference_link=true';

    fetch(url, {
      method: 'GET',
      credentials: 'same-origin',
      headers: {
        'Accept': 'application/json',
        'X-UserToken': token,
        'X-Requested-With': 'XMLHttpRequest'
      }
    })
      .then(function(response) {
        if (!response.ok) {
          throw new Error('Record query failed with status ' + response.status);
        }
        return response.json();
      })
      .then(function(payload) {
        var rows = Array.isArray(payload.result) ? payload.result : [];
        var records = [];
        for (var i = 0; i &lt; rows.length; i++) {
          var row = rows[i] || {};
          if (!row.sys_id) continue;
          records.push({
            sys_id: row.sys_id,
            display_value: String(row.name || row.sys_name || row.number || row.title || row.sys_id)
          });
        }
        records.sort(function(a, b) { return a.display_value.localeCompare(b.display_value); });
        recordCache[tableName] = records;

        // A stale response for a table the user has already navigated away
        // from still warms the cache, but must not clobber the current view
        if (activeTableName !== tableName) return;
        applyRecords(tableName, records);
      })
      .catch(function(error) {
        if (activeTableName !== tableName) return;
        resetRecordInput('Unable to load records', true);
        var failedInput = byId('guss-open-record-input');
        if (failedInput) failedInput.disabled = true;
        setHelp(String(error &amp;&amp; error.message ? error.message : error));
      });
  }

  function bindArtifactTypeSelect() {
    var select = byId('guss-open-modal-select');
    if (!select || select.getAttribute('data-bound') === 'true') return;
    select.setAttribute('data-bound', 'true');
    select.addEventListener('change', function() {
      loadRecordsForTable(this.value);
    });
  }

  function bindRecordInput() {
    var input = byId('guss-open-record-input');
    if (!input || input.getAttribute('data-bound') === 'true') return;
    input.setAttribute('data-bound', 'true');

    input.addEventListener('input', function() {
      // Editing invalidates any prior pick until a suggestion is clicked
      selectedRecord = null;
      setOpenSubmitVisible(false);
      renderSuggestions(this.value);
    });

    input.addEventListener('focus', function() {
      if (!selectedRecord) renderSuggestions(this.value);
    });
  }

  function openSelectedRecord() {
    var artifactTypeSelect = byId('guss-open-modal-select');
    var opener = window.GUSS_OPEN_RECORD_TAB;
    if (!artifactTypeSelect || !selectedRecord || typeof opener !== 'function') {
      return;
    }

    opener({
      tableName: artifactTypeSelect.value,
      targetSysId: selectedRecord.sys_id,
      name: selectedRecord.display_value
    });
    hideModal();
  }

  function loadLabels() {
    var select = byId('guss-open-modal-select');
    if (!select) return;
    select.innerHTML = '&lt;option value=""&gt;Loading labels...&lt;/option&gt;';
    activeTableName = '';
    activeRecords = [];
    resetRecordInput('Select an artifact type first', false);
    setHelp('');

	var live = window.GUSS_CREATE_ARTIFACTS;
	var entries = (Array.isArray(live) &amp;&amp; live.length)
		? live.map(function(item) { return { label: item.label, tableName: item.tableName }; })
		: parsePropertyValue((window.GUSS_CONTEXT || {}).createArtifactsJson || []);

    if (!entries.length) {
      select.innerHTML = '&lt;option value=""&gt;No artifacts available&lt;/option&gt;';
      resetRecordInput('No records available', false);
      setHelp('The create_artifacts property was not available or did not parse into valid artifact entries.');
      return;
    }

    fillSelect(entries);
    bindArtifactTypeSelect();
    bindRecordInput();
  }

  function showModal() {
    var overlay = byId('guss-open-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'flex';
    loadLabels();
  }

  function hideModal() {
    var overlay = byId('guss-open-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'none';
    hideSuggestions();
  }

  function bindOpenButton() {
    var button = byId('guss-open-btn');
    if (!button || button.getAttribute('data-open-modal-bound') === 'true') return;
    button.setAttribute('data-open-modal-bound', 'true');
    button.addEventListener('click', function (evt) {
      evt.preventDefault();
      showModal();
    });
  }

  function bindModalEvents() {
    var closeButton = byId('guss-open-modal-close');
    var cancelButton = byId('guss-open-modal-cancel');
    var submitButton = byId('guss-open-modal-submit');
    var overlay = byId('guss-open-modal-overlay');

    if (closeButton &amp;&amp; closeButton.getAttribute('data-close-bound') !== 'true') {
      closeButton.setAttribute('data-close-bound', 'true');
      closeButton.addEventListener('click', hideModal);
    }

    if (cancelButton &amp;&amp; cancelButton.getAttribute('data-cancel-bound') !== 'true') {
      cancelButton.setAttribute('data-cancel-bound', 'true');
      cancelButton.addEventListener('click', hideModal);
    }

    if (submitButton &amp;&amp; submitButton.getAttribute('data-submit-bound') !== 'true') {
      submitButton.setAttribute('data-submit-bound', 'true');
      submitButton.addEventListener('click', openSelectedRecord);
    }

    if (overlay &amp;&amp; overlay.getAttribute('data-overlay-bound') !== 'true') {
      overlay.setAttribute('data-overlay-bound', 'true');
      overlay.addEventListener('click', function (evt) {
        if (evt.target === overlay) hideModal();
      });
    }

    if (document.body &amp;&amp; document.body.getAttribute('data-open-modal-esc-bound') !== 'true') {
      document.body.setAttribute('data-open-modal-esc-bound', 'true');
      document.addEventListener('keydown', function (evt) {
        if (evt.key === 'Escape') hideModal();
      });
    }

    // Clicking anywhere outside the typeahead dismisses its suggestion list
    if (document.body &amp;&amp; document.body.getAttribute('data-open-suggest-bound') !== 'true') {
      document.body.setAttribute('data-open-suggest-bound', 'true');
      document.addEventListener('click', function (evt) {
        var input = byId('guss-open-record-input');
        var suggestions = byId('guss-open-record-suggestions');
        if (!suggestions) return;
        if (evt.target === input || suggestions.contains(evt.target)) return;
        suggestions.style.display = 'none';
      });
    }
  }

  // No MutationObserver here: the button and modal overlay are static markup
  // from the page's HTML field, never destroyed/recreated by any other
  // script on this page, so a one-time bind is all that's ever needed.
  function init() {
    bindOpenButton();
    bindModalEvents();
    bindRecordInput();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  }
  else {
    init();
  }
})();

// ── "Search Artifacts" modal ────────────────────────────────────────────────
// Two search modes, both scoped to the currently loaded update set:
//   1. File names   - instant, filters window.GUSS_SEARCH_INDEX (kept up to
//      date by loadTree()) client-side as the user types.
//   2. File content - debounced server query against sys_update_xml.payload
//      (sysparm_query ...^payloadCONTAINS&lt;term&gt;) so text inside scripts/XML
//      bodies is searchable too, not just artifact names.
// Either kind of result opens via the shared GUSS_OPEN_RECORD_TAB hook.
(function () {
  function byId(id) { return document.getElementById(id); }

  var CONTENT_FETCH_LIMIT  = 25;
  var CONTENT_DEBOUNCE_MS  = 300;
  var CONTENT_MIN_CHARS    = 3;

  var contentSearchTimer = null;
  var contentSearchToken = 0;

  // Shared with the main script IIFE above rather than private copies -
  // parseTargetFromName() there also validates the table-name shape (this
  // name comes from update-set data, which can originate from an
  // imported/applied XML file, and tableName ends up in a URL via
  // GUSS_OPEN_RECORD_TAB -&gt; buildRecordUrl).
  var parseTargetFromName = window.GUSS_UTIL.parseTargetFromName;
  var normalizeLabel = window.GUSS_UTIL.normalizeLabel;
  var esc = window.GUSS_UTIL.esc;

  // sys_update_xml.payload is the raw serialized XML of the target record,
  // so plain-text field values can show up XML-entity-escaped (e.g. "&amp;lt;"
  // for a literal "&lt;") even though script bodies are typically CDATA-wrapped
  // and unaffected. Decoding before building the snippet keeps the displayed
  // text readable. Order matters: decode everything else before "&amp;amp;" so
  // a literal "&amp;amp;lt;" in the source doesn't get double-unescaped into "&lt;".
  function decodeXmlEntities(text) {
    return String(text || '')
      .replace(/&amp;lt;/g, '&lt;')
      .replace(/&amp;gt;/g, '&gt;')
      .replace(/&amp;quot;/g, '"')
      .replace(/&amp;apos;/g, "'")
      .replace(/&amp;#39;/g, "'")
      .replace(/&amp;amp;/g, '&amp;');
  }

  // Build a short escaped snippet of payload text centered on the first
  // match, with the match itself wrapped in &lt;mark&gt;. Every piece of text is
  // escaped before assembly, so the result is safe to assign via innerHTML.
  // Note: the CONTAINS query on the server still matches against the raw
  // (possibly entity-escaped) payload, so a search term containing literal
  // "&lt;"/"&gt;"/"&amp;" can still miss content that isn't CDATA-wrapped - decoding
  // here only cleans up what's displayed for matches that were found.
  function buildSnippetHtml(payload, term) {
    var text = decodeXmlEntities(String(payload || ''));
    var idx = text.toLowerCase().indexOf(term.toLowerCase());
    if (idx === -1) return '';

    var radius = 40;
    var start = Math.max(0, idx - radius);
    var end = Math.min(text.length, idx + term.length + radius);

    var prefix = start &gt; 0 ? '…' : '';
    var suffix = end &lt; text.length ? '…' : '';
    var before = esc(text.slice(start, idx));
    var match = esc(text.slice(idx, idx + term.length));
    var after = esc(text.slice(idx + term.length, end));

    return prefix + before + '&lt;mark&gt;' + match + '&lt;/mark&gt;' + after + suffix;
  }

  function getSessionToken() {
    var ctx = window.GUSS_CONTEXT || {};
    return ctx.userToken || window.g_ck || '';
  }

  function setStatus(text) {
    var statusEl = byId('guss-search-status');
    if (statusEl) statusEl.textContent = text || '';
  }

  function openResult(item) {
    if (!item || !item.tableName || !item.targetSysId) return;
    var opener = window.GUSS_OPEN_RECORD_TAB;
    if (typeof opener !== 'function') return;
    opener({ tableName: item.tableName, targetSysId: item.targetSysId, name: item.name });
    hideModal();
  }

  function buildResultRow(item, isActive, snippetHtml) {
    var row = document.createElement('div');
    row.className = 'guss-search-result' + (isActive ? ' guss-search-active' : '');
    row.setAttribute('role', 'option');

    var rowInner = document.createElement('div');
    rowInner.className = 'guss-search-result-row';

    var head = document.createElement('div');
    head.className = 'guss-search-result-head';

    var nameEl = document.createElement('span');
    nameEl.className = 'guss-search-result-name';
    nameEl.textContent = item.name;
    head.appendChild(nameEl);

    if (item.typeName) {
      var typeEl = document.createElement('span');
      typeEl.className = 'guss-search-result-type';
      typeEl.textContent = item.typeName;
      head.appendChild(typeEl);
    }

    rowInner.appendChild(head);

    if (snippetHtml) {
      var snippetEl = document.createElement('div');
      snippetEl.className = 'guss-search-result-snippet';
      snippetEl.innerHTML = snippetHtml;
      rowInner.appendChild(snippetEl);
    }

    row.appendChild(rowInner);
    row.addEventListener('click', function() { openResult(item); });
    return row;
  }

  // Render file-name matches into `container`. Returns the full (unsliced)
  // match list so the caller can dedupe content-search results against it.
  function renderNameSection(container, query) {
    var index = Array.isArray(window.GUSS_SEARCH_INDEX) ? window.GUSS_SEARCH_INDEX : [];
    container.innerHTML = '';

    if (!index.length) {
      var emptyIndex = document.createElement('div');
      emptyIndex.className = 'guss-search-empty-msg';
      emptyIndex.textContent = 'No artifacts loaded for the current update set.';
      container.appendChild(emptyIndex);
      return [];
    }

    var trimmed = String(query || '').trim().toLowerCase();
    var matches = !trimmed ? index : index.filter(function(item) {
      return item.name.toLowerCase().indexOf(trimmed) !== -1;
    });

    if (trimmed) {
      var title = document.createElement('div');
      title.className = 'guss-search-section-title';
      title.textContent = 'File Names';
      container.appendChild(title);
    }

    if (!matches.length) {
      var emptyMatches = document.createElement('div');
      emptyMatches.className = 'guss-search-empty-msg';
      emptyMatches.textContent = 'No file name matches.';
      container.appendChild(emptyMatches);
      return matches;
    }

    matches.slice(0, 100).forEach(function(item, idx) {
      container.appendChild(buildResultRow(item, idx === 0, null));
    });

    return matches;
  }

  function renderContentSection(container, matches, excludeIds) {
    container.innerHTML = '';

    var title = document.createElement('div');
    title.className = 'guss-search-section-title';
    title.textContent = 'File Content';
    container.appendChild(title);

    var filtered = matches.filter(function(item) {
      return !excludeIds.has(item.updateXmlSysId);
    });

    if (!filtered.length) {
      var empty = document.createElement('div');
      empty.className = 'guss-search-empty-msg';
      empty.textContent = 'No content matches.';
      container.appendChild(empty);
      return;
    }

    filtered.forEach(function(item) {
      container.appendChild(buildResultRow(item, false, item.snippetHtml));
    });
  }

  // Query sys_update_xml directly for payload text containing `term`,
  // scoped to the update set currently loaded in the sidebar tree.
  function fetchContentMatches(term) {
    var updateSetId = window.GUSS_CURRENT_UPDATE_SET_ID || '';
    if (!updateSetId) return Promise.resolve([]);

    // ServiceNow's encoded-query format uses "^" as the condition separator
    // and has no escape sequence for a literal "^" inside a value. Passing
    // the raw search term through would let "^" in user input inject extra
    // query conditions (e.g. widen the scope past this update_set, or knock
    // out the action!=DELETE filter). Strip it before it ever reaches the
    // query string; there's no way to search for a literal "^" anyway.
    var safeTerm = term.replace(/\^/g, '');
    if (!safeTerm) return Promise.resolve([]);

    var query = encodeURIComponent('update_set=' + updateSetId + '^action!=DELETE^payloadCONTAINS' + safeTerm);
    var fields = encodeURIComponent('sys_id,type,target_name,name,action,payload');
    // sysparm_no_count skips computing a total row count for this query, which
    // is otherwise wasted work on a CONTAINS scan of an unindexed long-text
    // column (payload) - a real cost saver even though the scan itself can't
    // be made cheaper from a client script.
    var url = '/api/now/table/sys_update_xml?sysparm_display_value=true&amp;sysparm_exclude_reference_link=true&amp;sysparm_no_count=true&amp;sysparm_limit=' + CONTENT_FETCH_LIMIT + '&amp;sysparm_query=' + query + '&amp;sysparm_fields=' + fields;

    return fetch(url, {
      method: 'GET',
      credentials: 'same-origin',
      headers: {
        'Accept': 'application/json',
        'X-UserToken': getSessionToken(),
        'X-Requested-With': 'XMLHttpRequest'
      }
    })
      .then(function(response) {
        if (!response.ok) throw new Error('Content search failed with status ' + response.status);
        return response.json();
      })
      .then(function(payload) {
        var rows = Array.isArray(payload.result) ? payload.result : [];
        return rows.map(function(row) {
          var parsed = parseTargetFromName(row.name || '');
          return {
            name: normalizeLabel(row),
            typeName: String(row.type || '').trim(),
            tableName: parsed.tableName,
            targetSysId: parsed.targetSysId,
            updateXmlSysId: row.sys_id || '',
            snippetHtml: buildSnippetHtml(row.payload, safeTerm)
          };
        });
      });
  }

  function ensureSections() {
    var resultsEl = byId('guss-search-results');
    if (!resultsEl) return null;
    resultsEl.innerHTML =
      '&lt;div id="guss-search-name-section"&gt;&lt;/div&gt;' +
      '&lt;div id="guss-search-content-section"&gt;&lt;/div&gt;';
    return {
      nameSection: byId('guss-search-name-section'),
      contentSection: byId('guss-search-content-section')
    };
  }

  // Renders name matches immediately, then debounces a content search
  // against sys_update_xml.payload once the query is long enough.
  function performSearch(query) {
    var sections = ensureSections();
    if (!sections) return;

    var nameMatches = renderNameSection(sections.nameSection, query);
    var nameIds = new Set(
      nameMatches.map(function(m) { return m.updateXmlSysId; }).filter(Boolean)
    );

    if (contentSearchTimer) {
      clearTimeout(contentSearchTimer);
      contentSearchTimer = null;
    }
    contentSearchToken += 1; // invalidate any in-flight content request

    var trimmed = String(query || '').trim();
    setStatus('');

    if (trimmed.length &lt; CONTENT_MIN_CHARS) {
      if (trimmed.length &gt; 0) setStatus('Type at least ' + CONTENT_MIN_CHARS + ' characters to search file content.');
      return;
    }

    var requestId = contentSearchToken;
    setStatus('Searching content…');

    contentSearchTimer = setTimeout(function() {
      fetchContentMatches(trimmed)
        .then(function(matches) {
          if (requestId !== contentSearchToken) return; // superseded by a newer query
          setStatus(matches.length
            ? matches.length + ' content match' + (matches.length === 1 ? '' : 'es') +
              (matches.length === CONTENT_FETCH_LIMIT ? ' (showing first ' + CONTENT_FETCH_LIMIT + ')' : '')
            : 'No content matches.');
          renderContentSection(sections.contentSection, matches, nameIds);
          if (!nameMatches.length &amp;&amp; matches.length) {
            var first = sections.contentSection.querySelector('.guss-search-result');
            if (first) first.classList.add('guss-search-active');
          }
        })
        .catch(function(error) {
          if (requestId !== contentSearchToken) return;
          setStatus('Content search failed: ' + (error &amp;&amp; error.message ? error.message : error));
        });
    }, CONTENT_DEBOUNCE_MS);
  }

  function showModal() {
    var overlay = byId('guss-search-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'flex';
    var input = byId('guss-search-input');
    if (input) {
      input.value = '';
      setTimeout(function() { input.focus(); }, 0);
    }
    performSearch('');
  }

  function hideModal() {
    var overlay = byId('guss-search-modal-overlay');
    if (!overlay) return;
    overlay.style.display = 'none';
    if (contentSearchTimer) {
      clearTimeout(contentSearchTimer);
      contentSearchTimer = null;
    }
    contentSearchToken += 1; // ignore any response that arrives after close
  }

  function bindSearchButton() {
    var button = byId('guss-search-btn');
    if (!button || button.getAttribute('data-search-modal-bound') === 'true') return;
    button.setAttribute('data-search-modal-bound', 'true');
    button.addEventListener('click', function (evt) {
      evt.preventDefault();
      showModal();
    });
  }

  function bindModalEvents() {
    var closeButton = byId('guss-search-modal-close');
    var overlay = byId('guss-search-modal-overlay');
    var input = byId('guss-search-input');

    if (closeButton &amp;&amp; closeButton.getAttribute('data-close-bound') !== 'true') {
      closeButton.setAttribute('data-close-bound', 'true');
      closeButton.addEventListener('click', hideModal);
    }

    if (overlay &amp;&amp; overlay.getAttribute('data-overlay-bound') !== 'true') {
      overlay.setAttribute('data-overlay-bound', 'true');
      overlay.addEventListener('click', function (evt) {
        if (evt.target === overlay) hideModal();
      });
    }

    if (input &amp;&amp; input.getAttribute('data-input-bound') !== 'true') {
      input.setAttribute('data-input-bound', 'true');
      input.addEventListener('input', function() {
        performSearch(this.value);
      });
      input.addEventListener('keydown', function(evt) {
        if (evt.key === 'Enter') {
          var first = document.querySelector('#guss-search-results .guss-search-result');
          if (first) first.click();
        }
      });
    }

    if (document.body &amp;&amp; document.body.getAttribute('data-search-modal-esc-bound') !== 'true') {
      document.body.setAttribute('data-search-modal-esc-bound', 'true');
      document.addEventListener('keydown', function (evt) {
        if (evt.key === 'Escape') hideModal();
      });
    }
  }

  // No MutationObserver here: the button and modal overlay are static markup
  // from the page's HTML field, never destroyed/recreated by any other
  // script on this page, so a one-time bind is all that's ever needed.
  function init() {
    bindSearchButton();
    bindModalEvents();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  }
  else {
    init();
  }
})();
]]&gt;&lt;/client_script&gt;&lt;description&gt;Landing page for Global Update Set Studio.&lt;/description&gt;&lt;direct&gt;true&lt;/direct&gt;&lt;endpoint/&gt;&lt;html&gt;&lt;![CDATA[&lt;j:jelly xmlns:j="jelly:core" xmlns:g="glide"&gt;

&lt;style&gt;
:root {
  --bg: #111827;
  --panel: #1f2937;
  --panel-2: #0f172a;
  --text: #e5e7eb;
  --muted: #9ca3af;
  --border: #374151;
  --hover: #243244;
  --selected: #1e3a5f;
  --warning-bg: #3f2a12;
  --warning-border: #7c4a13;
  --warning-text: #fcd9a3;
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  font-family: Inter, Arial, sans-serif;
  background: var(--bg);
  color: var(--text);
}

.guss-app {
  display: grid;
  grid-template-columns: var(--guss-sidebar-width, 330px) 6px 1fr;
  height: 100vh;
}

.guss-sidebar {
  border-right: 1px solid var(--border);
  background: var(--panel-2);
  overflow: auto;
  position: relative;
}

.guss-resizer {
  background: #1f2937;
  border-right: 1px solid var(--border);
  cursor: ew-resize;
  position: relative;
}

.guss-resizer:hover {
  background: #334155;
}

.guss-resizer::after {
  content: '';
  position: absolute;
  left: 2px;
  top: 50%;
  width: 2px;
  height: 48px;
  transform: translateY(-50%);
  background: rgba(148, 163, 184, 0.75);
  border-radius: 1px;
}

.guss-sidebar-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 12px 8px;
  position: sticky;
  top: 0;
  background: var(--panel-2);
  z-index: 5;
}

.guss-add-btn {
  width: 30px;
  height: 30px;
  border-radius: 8px;
  border: 2px solid rgba(255, 255, 255, 0.25);
  background: rgba(255, 255, 255, 0.12);
  color: #ffffff;
  font-size: 18px;
  line-height: 1;
  font-weight: 600;
  cursor: pointer;
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08);
}

.guss-add-btn:hover {
  background: rgba(255, 255, 255, 0.18);
}

#guss-add-modal-overlay {
  position: fixed;
  inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.45);
  z-index: 9999;
}

#guss-add-modal {
  width: 340px;
  max-width: calc(100vw - 32px);
  background: #131922;
  color: #ffffff;
  border: 1px solid rgba(255, 255, 255, 0.16);
  border-radius: 8px;
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.45);
  padding: 16px;
}

#guss-add-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

#guss-add-modal-title {
  font-size: 16px;
  font-weight: 600;
}

#guss-add-modal-close {
  border: 0;
  background: transparent;
  color: #d6dde8;
  font-size: 18px;
  cursor: pointer;
  line-height: 1;
}

#guss-add-modal-list {
  max-height: 360px;
  overflow-y: auto;
}

.guss-add-modal-item {
  display: flex;
  align-items: center;
  gap: 10px;
  width: 100%;
  border: 0;
  background: transparent;
  color: #ffffff;
  padding: 9px 8px;
  border-radius: 4px;
  font-size: 13px;
  text-align: left;
  cursor: pointer;
}

.guss-add-modal-item:hover {
  background: #1e3a5f;
}

.guss-add-modal-icon {
  width: 20px;
  text-align: center;
  font-size: 16px;
  line-height: 1;
}

.guss-add-modal-empty {
  padding: 10px 2px;
  font-size: 12px;
  color: #9fb0c6;
}

.guss-main {
  display: grid;
  grid-template-rows: 48px 1fr;
  min-width: 0;
  min-height: 0;
}

.guss-topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 0 14px;
  border-bottom: 1px solid var(--border);
  background: var(--panel);
}

.guss-title {
  font-weight: 600;
  letter-spacing: 0.2px;
}

.guss-topbar-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.guss-meta {
  color: var(--muted);
  font-size: 12px;
  white-space: nowrap;
}

.guss-meta-link {
  color: var(--muted);
  text-decoration: none;
}

.guss-meta-link:hover {
  color: var(--text);
  text-decoration: underline;
}

.guss-btn {
  border: 1px solid var(--border);
  background: #0b1220;
  color: var(--text);
  border-radius: 6px;
  padding: 6px 10px;
  font-size: 12px;
  cursor: pointer;
}

.guss-btn:hover {
  background: #172033;
}

.guss-workspace {
  display: grid;
  grid-template-rows: 1fr;
  min-height: 0;
  overflow: hidden;
}

.guss-tabs {
  display: flex;
  /* align-items: flex-end; */
  gap: 2px;
  padding: 0;
  border-bottom: 1px solid #bcc7d5;
  background: #dde4ec;
  overflow-x: auto;
}

.guss-tab {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 0;
  padding: 16px 14px 13px;
  border: 1px solid transparent;
  border-bottom: none;
  border-top-left-radius: 14px;
  border-top-right-radius: 14px;
  background: #d5dde7;
  color: #5b6f87;
  cursor: pointer;
  white-space: nowrap;
  font-size: 12px;
  line-height: 1;
  position: relative;
  top: -1px;
}

.guss-tab.active {
  background: #ffffff;
  color: #0f4f79;
  border-color: #b8c5d4;
  box-shadow: inset 0 3px 0 #1ea6ff;
}

.guss-tab-label {
  max-width: 230px;
  overflow: hidden;
  text-overflow: ellipsis;
}

.guss-tab-close {
  color: #8a99ac;
  font-weight: 600;
  font-size: 22px;
  line-height: 12px;
  margin-left: 2px;
}

.guss-viewer {
  min-height: 0;
  overflow: hidden;
}

.guss-iframe {
  width: 100%;
  height: 100%;
  border: 0;
  background: #fff;
}

.guss-empty {
  height: 100%;
  padding: 16px;
  overflow: auto;
}

.guss-empty-intro {
  text-align: center;
}

.guss-empty-title {
  font-size: 56px;
  font-weight: 700;
  line-height: 1;
  color: #e5e7eb;
  letter-spacing: 1px;
}

.guss-empty-subtitle {
  margin-top: 12px;
  font-size: 20px;
  color: #9ca3af;
}

.guss-empty-divider {
  width: 220px;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.55),
    transparent
  );
  margin: 16px auto 0;
}

.guss-empty-meta {
  margin: 16px auto 0;
  display: grid;
  grid-template-columns: 150px minmax(280px, 1fr);
  gap: 12px 16px;
  align-items: center;
  justify-content: center;
  width: min(100%, 920px);
  max-width: 920px;
}

.guss-empty-label {
  font-size: 18px;
  color: #cbd5e1;
  text-align: right;
  white-space: nowrap;
}

.guss-empty-value {
  font-size: 18px;
  color: #cbd5e1;
  text-align: left;
}

.guss-empty-note {
  display: block;
  width: min(100%, 600px);
  margin: 20px auto 0;
  padding: 12px 20px;
  border-radius: 10px;
  border: 1px solid rgba(20, 184, 166, 0.45);
  background: rgba(13, 148, 136, 0.18);
  color: #99f6e4;
  font-size: 14px;
  font-style: italic;
  line-height: 1.5;
  text-align: center;
}

.guss-empty-select {
  width: 100%;
  padding: 9px 14px;
  border-radius: 4px;
  border: 1px solid #94a3b8;
  background: linear-gradient(180deg, #ffffff 0%, #eef3f8 100%);
  color: #0f172a;
  font-size: 16px;
  font-weight: 500;
  box-shadow:
    0 8px 20px rgba(15, 23, 42, 0.18),
    inset 0 1px 0 rgba(255, 255, 255, 0.95);
}

.guss-empty-select:focus {
  outline: none;
  border-color: #60a5fa;
  box-shadow:
    0 0 0 3px rgba(59, 130, 246, 0.25),
    0 8px 20px rgba(15, 23, 42, 0.18),
    inset 0 1px 0 rgba(255, 255, 255, 0.95);
}

.guss-empty-select:disabled {
  opacity: 0.7;
  cursor: default;
}

.guss-empty-action-cell {
  width: min(100%, 920px);
  max-width: 920px;
  margin: 12px auto 0;
  display: flex;
  justify-content: flex-end;
}

.guss-empty-action {
  padding: 8px 12px;
  border: 1px solid #60a5fa;
  border-radius: 10px;
  background: linear-gradient(180deg, #dbeafe 0%, #bfdbfe 100%);
  color: #0f172a;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 4px 10px rgba(15, 23, 42, 0.12);
}

.guss-empty-action:hover {
  background: linear-gradient(180deg, #bfdbfe 0%, #93c5fd 100%);
}

.guss-loading-wrap {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.guss-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 14px;
  color: #cbd5e1;
}

.guss-spinner {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  border: 4px solid rgba(148, 163, 184, 0.28);
  border-top-color: #93c5fd;
  animation: guss-spin 0.9s linear infinite;
}

.guss-loading-text {
  font-size: 14px;
  letter-spacing: 0.03em;
  color: #94a3b8;
}

@keyframes guss-spin {
  from {
    transform: rotate(0deg);
  }

  to {
    transform: rotate(360deg);
  }
}

.guss-panel {
  background: #0b1220;
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 14px;
}

.guss-muted {
  color: var(--muted);
}

.guss-notice {
  margin: 8px 12px 0;
  border: 1px solid var(--warning-border);
  background: var(--warning-bg);
  color: var(--warning-text);
  border-radius: 6px;
  padding: 8px 10px;
  font-size: 12px;
  display: none;
}

.guss-section-title {
  padding: 12px 12px 8px;
  font-size: 12px;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.guss-sidebar-actions {
  display: flex;
  align-items: center;
  flex: 0 0 auto;
  flex-wrap: nowrap;
}

.guss-tree {
  list-style: none;
  margin: 0;
  padding: 0 8px 16px;
}

.guss-node {
  margin: 1px 0;
}

.guss-node-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 8px;
  border-radius: 6px;
  cursor: pointer;
  user-select: none;
}

.guss-node-row:hover {
  background: var(--hover);
}

.guss-node-row.guss-selected {
  background: var(--selected);
}

.guss-caret {
  width: 10px;
  color: var(--muted);
  flex: 0 0 10px;
  text-align: center;
}

.guss-name {
  flex: 1;
  font-size: 12px;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.guss-children {
  list-style: none;
  margin: 0;
  padding: 0 0 0 22px;
}

.guss-file-icon {
  color: #93c5fd;
}

.guss-folder-icon {
  color: #fbbf24;
}

#guss-open-btn.guss-open-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-left: -6px;
  width: 34px;
  min-width: 34px;
  padding: 0;
  box-sizing: border-box;
  height: 34px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.22);
  background: #1b1b1b;
  color: #ffffff;
  line-height: 1;
  cursor: pointer;
}

#guss-open-btn.guss-open-btn:hover {
  background: #242424;
}

#guss-open-btn .guss-open-icon {
  display: inline-block;
  font-size: 16px;
  line-height: 1;
}

#guss-search-btn.guss-search-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-left: 8px;
  width: 34px;
  min-width: 34px;
  padding: 0;
  box-sizing: border-box;
  height: 34px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.22);
  background: #1b1b1b;
  color: #ffffff;
  line-height: 1;
  cursor: pointer;
}

#guss-search-btn.guss-search-btn:hover {
  background: #242424;
}

#guss-search-btn .guss-search-icon {
  display: inline-block;
  font-size: 16px;
  line-height: 1;
}

#guss-add-btn.guss-new-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-left: 8px;
  margin-right: 30px;
  transform: translateX(16px);
  width: 34px;
  min-width: 34px;
  padding: 0;
  box-sizing: border-box;
  height: 34px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.22);
  background: #1b1b1b;
  color: #ffffff;
  line-height: 1;
  cursor: pointer;
}

#guss-add-btn.guss-new-btn:hover {
  background: #242424;
}

#guss-add-btn .guss-new-icon {
  display: inline-block;
  font-size: 16px;
  line-height: 1;
  font-weight: 600;
}

#guss-open-modal-overlay {
  position: fixed;
  inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.45);
  z-index: 9999;
}

#guss-open-modal {
  width: 460px;
  max-width: calc(100vw - 32px);
  background: #131922;
  color: #ffffff;
  border: 1px solid rgba(255, 255, 255, 0.16);
  border-radius: 8px;
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.45);
  padding: 16px;
}

#guss-open-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

#guss-open-modal-title {
  font-size: 16px;
  font-weight: 600;
}

#guss-open-modal-close {
  border: 0;
  background: transparent;
  color: #d6dde8;
  font-size: 18px;
  cursor: pointer;
  line-height: 1;
}

#guss-open-modal-label {
  display: block;
  font-size: 12px;
  font-weight: 600;
  margin-bottom: 6px;
  color: #cbd5e1;
}

#guss-open-modal-select,
#guss-open-record-input {
  width: 100%;
  height: 34px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.18);
  background: #0f1722;
  color: #ffffff;
  padding: 0 8px;
  font-size: 12px;
}

#guss-open-record-input:disabled {
  opacity: 0.6;
}

#guss-open-record-wrap {
  position: relative;
}

#guss-open-record-suggestions {
  display: none;
  position: absolute;
  left: 0;
  right: 0;
  top: 100%;
  margin-top: 4px;
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 4px;
  background: #0f1722;
  z-index: 10000;
  max-height: 200px;
  overflow-y: auto;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.45);
}

.guss-open-suggestion {
  padding: 8px 10px;
  cursor: pointer;
  font-size: 12px;
  color: #ffffff;
}

.guss-open-suggestion:hover {
  background: #1e3a5f;
}

#guss-open-record-label {
  display: block;
  font-size: 12px;
  font-weight: 600;
  margin-top: 12px;
  margin-bottom: 6px;
  color: #cbd5e1;
}

#guss-open-modal-help {
  margin-top: 8px;
  font-size: 11px;
  color: #9fb0c6;
  min-height: 14px;
}

#guss-open-modal-actions {
  margin-top: 14px;
  display: flex;
  justify-content: flex-end;
  gap: 8px;
}

.guss-open-modal-btn {
  height: 30px;
  padding: 0 12px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.24);
  background: #1b1b1b;
  color: #ffffff;
  font-size: 12px;
  cursor: pointer;
}

#guss-search-modal-overlay {
  position: fixed;
  inset: 0;
  display: none;
  align-items: flex-start;
  justify-content: center;
  padding-top: 90px;
  background: rgba(0, 0, 0, 0.45);
  z-index: 9999;
}

#guss-search-modal {
  width: 480px;
  max-width: calc(100vw - 32px);
  background: #131922;
  color: #ffffff;
  border: 1px solid rgba(255, 255, 255, 0.16);
  border-radius: 8px;
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.45);
  padding: 16px;
}

#guss-search-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

#guss-search-modal-title {
  font-size: 16px;
  font-weight: 600;
}

#guss-search-modal-close {
  border: 0;
  background: transparent;
  color: #d6dde8;
  font-size: 18px;
  cursor: pointer;
  line-height: 1;
}

#guss-search-input {
  width: 100%;
  height: 34px;
  border-radius: 4px;
  border: 1px solid rgba(255, 255, 255, 0.18);
  background: #0f1722;
  color: #ffffff;
  padding: 0 8px;
  font-size: 13px;
}

#guss-search-results {
  margin-top: 10px;
  max-height: 320px;
  overflow-y: auto;
}

.guss-search-result {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 10px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 13px;
}

.guss-search-result:hover,
.guss-search-result.guss-search-active {
  background: #1e3a5f;
}

.guss-search-result-name {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.guss-search-result-type {
  font-size: 11px;
  color: #9fb0c6;
  white-space: nowrap;
}

.guss-search-result-row {
  display: flex;
  flex-direction: column;
  gap: 2px;
  flex: 1;
  min-width: 0;
}

.guss-search-result-head {
  display: flex;
  align-items: center;
  gap: 8px;
}

.guss-search-result-snippet {
  font-size: 11px;
  color: #9fb0c6;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace;
}

.guss-search-result-snippet mark {
  background: #7c4a13;
  color: #fcd9a3;
  border-radius: 2px;
}

.guss-search-section-title {
  padding: 10px 2px 4px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: #9fb0c6;
}

#guss-search-status {
  margin-top: 8px;
  font-size: 11px;
  color: #9fb0c6;
  min-height: 14px;
}

.guss-search-empty-msg {
  padding: 10px 2px;
  font-size: 12px;
  color: #9fb0c6;
}
&lt;/style&gt;

&lt;!-- Server-provided context, handed off to Client Script as inert JSON data.
     No executable JavaScript lives in this HTML field anymore — this is a
     data island only. Client Script reads and parses it on load.
     JSON.stringify() (Rhino's built-in) does the escaping here rather than
     a hand-rolled .replace() chain, since the create_artifacts property
     value is itself JSON text full of quotes that must round-trip safely. --&gt;
&lt;script id="guss-context-data" type="application/json"&gt;
{
  "updateSetId": $[JSON.stringify(gs.getPreference('sys_update_set'))],
  "userToken": $[JSON.stringify(gs.getSessionToken())],
  "createArtifactsJson": $[JSON.stringify(gs.getProperty('global_update_set_studio.create_artifacts'))]
}
&lt;/script&gt;

&lt;div id="guss-open-modal-overlay" role="dialog" aria-modal="true" aria-labelledby="guss-open-modal-title"&gt;
  &lt;div id="guss-open-modal"&gt;
    &lt;div id="guss-open-modal-header"&gt;
      &lt;div id="guss-open-modal-title"&gt;Open File&lt;/div&gt;
      &lt;button id="guss-open-modal-close" type="button" aria-label="Close"&gt;×&lt;/button&gt;
    &lt;/div&gt;
    &lt;label id="guss-open-modal-label" for="guss-open-modal-select"&gt;File type&lt;/label&gt;
    &lt;select id="guss-open-modal-select"&gt;
      &lt;option value=""&gt;Loading labels...&lt;/option&gt;
    &lt;/select&gt;
    &lt;div id="guss-open-record-group" style="display:none;"&gt;
      &lt;label id="guss-open-record-label" for="guss-open-record-input"&gt;Record&lt;/label&gt;
      &lt;div id="guss-open-record-wrap"&gt;
        &lt;input type="text" id="guss-open-record-input" autocomplete="off" placeholder="Type to search records..." /&gt;
        &lt;div id="guss-open-record-suggestions" role="listbox"&gt;&lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;div id="guss-open-modal-help"&gt;&lt;/div&gt;
    &lt;div id="guss-open-modal-actions"&gt;
      &lt;button id="guss-open-modal-submit" class="guss-open-modal-btn" type="button" style="display:none;"&gt;Open&lt;/button&gt;
      &lt;button id="guss-open-modal-cancel" class="guss-open-modal-btn" type="button"&gt;Close&lt;/button&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;div id="guss-search-modal-overlay" role="dialog" aria-modal="true" aria-labelledby="guss-search-modal-title"&gt;
  &lt;div id="guss-search-modal"&gt;
    &lt;div id="guss-search-modal-header"&gt;
      &lt;div id="guss-search-modal-title"&gt;Search Artifacts&lt;/div&gt;
      &lt;button id="guss-search-modal-close" type="button" aria-label="Close"&gt;×&lt;/button&gt;
    &lt;/div&gt;
    &lt;input type="text" id="guss-search-input" autocomplete="off" placeholder="Search file names or content in this update set..." /&gt;
    &lt;div id="guss-search-status"&gt;&lt;/div&gt;
    &lt;div id="guss-search-results" role="listbox"&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;div id="guss-add-modal-overlay" role="dialog" aria-modal="true" aria-labelledby="guss-add-modal-title"&gt;
  &lt;div id="guss-add-modal"&gt;
    &lt;div id="guss-add-modal-header"&gt;
      &lt;div id="guss-add-modal-title"&gt;Add New File&lt;/div&gt;
      &lt;button id="guss-add-modal-close" type="button" aria-label="Close"&gt;×&lt;/button&gt;
    &lt;/div&gt;
    &lt;div id="guss-add-modal-list" role="listbox"&gt;&lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

&lt;div id="guss-app" class="guss-app"&gt;
  &lt;aside class="guss-sidebar"&gt;
    &lt;div class="guss-sidebar-head"&gt;
      &lt;div class="guss-section-title"&gt;Update Set Files&lt;/div&gt;

		&lt;div class="guss-sidebar-actions"&gt;
			&lt;button id="guss-add-btn" class="guss-add-btn guss-new-btn" type="button" aria-label="Add new artifact"&gt;&lt;span class="guss-new-icon"&gt;+&lt;/span&gt;&lt;/button&gt;
			&lt;button id="guss-open-btn" class="guss-open-btn" type="button" aria-label="Open update set"&gt;&lt;span class="guss-open-icon"&gt;]]&gt;📂&lt;![CDATA[&lt;/span&gt;&lt;/button&gt;
			&lt;button id="guss-search-btn" class="guss-search-btn" type="button" aria-label="Search"&gt;&lt;span class="guss-search-icon"&gt;]]&gt;🔍&lt;![CDATA[&lt;/span&gt;&lt;/button&gt;
		&lt;/div&gt;
    &lt;/div&gt;
    &lt;div id="guss-notice" class="guss-notice"&gt;&lt;/div&gt;
    &lt;ul id="guss-tree" class="guss-tree"&gt;&lt;/ul&gt;
  &lt;/aside&gt;
  &lt;div id="guss-sidebar-resizer" class="guss-resizer" role="separator" aria-orientation="vertical" aria-label="Resize sidebar"&gt;&lt;/div&gt;

  &lt;main class="guss-main"&gt;
    &lt;div class="guss-topbar"&gt;
      &lt;div class="guss-title"&gt;&lt;!-- This space intentionally left blank --&gt;&lt;/div&gt;
      &lt;div class="guss-topbar-actions"&gt;
        &lt;span id="guss-meta" class="guss-meta"&gt;Loading current update set...&lt;/span&gt;
        &lt;button id="guss-reload" class="guss-btn" type="button"&gt;Reload&lt;/button&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;section class="guss-workspace"&gt;
      &lt;div id="guss-tabs" class="guss-tabs"&gt;&lt;/div&gt;
      &lt;div id="guss-viewer" class="guss-viewer"&gt;&lt;/div&gt;
    &lt;/section&gt;
  &lt;/main&gt;
&lt;/div&gt;

&lt;/j:jelly&gt;]]&gt;&lt;/html&gt;&lt;name&gt;GUSS&lt;/name&gt;&lt;processing_script/&gt;&lt;sys_class_name&gt;sys_ui_page&lt;/sys_class_name&gt;&lt;sys_created_by&gt;svc.spie.mcpserver&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-07 23:50:39&lt;/sys_created_on&gt;&lt;sys_id&gt;6ec0bbb583a2cf9066b576d6feaad3be&lt;/sys_id&gt;&lt;sys_mod_count&gt;155&lt;/sys_mod_count&gt;&lt;sys_name&gt;GUSS&lt;/sys_name&gt;&lt;sys_package display_value="Global" source="global"&gt;global&lt;/sys_package&gt;&lt;sys_policy/&gt;&lt;sys_scope display_value="Global"&gt;global&lt;/sys_scope&gt;&lt;sys_update_name&gt;sys_ui_page_6ec0bbb583a2cf9066b576d6feaad3be&lt;/sys_update_name&gt;&lt;sys_updated_by&gt;nervoussow&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-18 13:55:49&lt;/sys_updated_on&gt;&lt;/sys_ui_page&gt;&lt;sys_es_latest_script action="INSERT_OR_UPDATE"&gt;&lt;id&gt;6ec0bbb583a2cf9066b576d6feaad3be&lt;/id&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-09 18:01:20&lt;/sys_created_on&gt;&lt;sys_id&gt;0d04bc6a83a287d066b576d6feaad302&lt;/sys_id&gt;&lt;sys_mod_count&gt;0&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-08-09 18:01:20&lt;/sys_updated_on&gt;&lt;table&gt;sys_ui_page&lt;/table&gt;&lt;use_es_latest&gt;true&lt;/use_es_latest&gt;&lt;/sys_es_latest_script&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1859776434</payload_hash>
<remote_update_set display_value="GlobalUpdateStudio-v2.2">90892690832f4bd466b576d6feaad3a8</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>nervoussow</sys_created_by>
<sys_created_on>2026-09-21 19:51:06</sys_created_on>
<sys_id>98892690832f4bd466b576d6feaad3a9</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a0b4cd510d0000001</sys_recorded_at>
<sys_updated_by>nervoussow</sys_updated_by>
<sys_updated_on>2026-09-21 19:51:06</sys_updated_on>
<table/>
<target_name>GUSS</target_name>
<type>UI Page</type>
<update_domain>global</update_domain>
<update_guid>626b31df78130f94f8debc70dfd6381a</update_guid>
<update_guid_history>626b31df78130f94f8debc70dfd6381a:-1859776434,db5bf99f9d130f9436ae14566347184a:709571479,77e365c717d70b943882e22b5274cce3:-1859776434,e6d1e5cf12970b94ac3cccd8b382fb00:843853377,4ac0e54b9d970b94a4bb6229640bed97:326480197,cfab1143c1570b946a389c7564426557:-207841999,4ed915079e570b949a977c560a79c56d:-655540087,41cd580ffb1bc794699b7a9a8b585cb2:355444744,71e1eaf66ddf4794b51ef3b06611334c:1707036181,cbcc52f6129f479401e72b63bce22389:-407900578,c1805abed0db4794cf6e5c1741311492:982037248,f824c8ea9b9b0394177eff87ddf339b8:-593830670,f1c89e46ca5fcb54ca47750d2c6fe404:-1902017325,e454cde4109b8714550dfe5f7d23bad1:-1733121715,7de389a46d9b8714e01d16ad3e64b846:189996134,2c630164d69b87143af2cec158b4c43c:-1811767910,991349e0c69b871478299ba14c51b00c:810614470,e82d3c208c5b8714668f0a90af120d8e:1524705197,a317c424a79f4714f4f0a17f3ad0ff28:236492006,77ac1f1caf574714f60812265f8d904e:693921862,917c971c7e574714c0a0c6b5472f478e:-936386174,15360d1c8ad7c314ce912e9e01ae672c:-466228237,21adae7c02cbc75028e61623413f8417:-936386174,f56dea7cb0cbc75018434cad96a276d3:-531814214,f6cc2ef8f6cbc750ea9467e44bc6df1c:977590454,0eab62383bcbc7509d3b846b04a5cad4:-1802412199,3b7b6af455cbc750df1f3b427a07ce02:-463265445,b5cb28b8b3438750c809ddffb056af5d:-1802412199,f61b6038b7438750e475377deffe30df:-562334491,d61a2834c04387501d8e21a4449e8017:-1827332804,eed964342343875047443fe811874d1a:-1470154519,e1a9a8f0e343875071b84d32d99e9f4a:-1919226361,0429ec701d4387505278374c7ea7a39c:-1467828010,756824f8a0cf47500872e5412631e052:-1814621945,0ae624349003875020e8b51d2f4a9fc3:-1699972373,7cb4acf0d4038750ac0816dd11465bf6:-1355187324,5c4428b0e4038750d61aaec9e889b402:1113578601,b8bb88f083cb4750b084b3f6f217876a:499164947,b07b84f02bcb4750bdc3e50ab0500c0d:173693981,ba97c074568b47505f84975124517610:1113578601,253784f05a8b47507b3d38d5b713fba2:1575989485,92c432231d7ac710f318e17a93b81a75:967851497,3f3ef19f3bfac31077facbe169c62851:-1640504450,e579611f42f6c310d29bcafb56fd3905:1398932889,a729e59be2f6c3105c11dc9dad857d01:537961678,5283d99799f2c3102391881dcc392a67:-888203311,72c5c5dbf432c3103e3de5979660b890:1705641894,61e4c91bf932c3103e23d8dde19cb21e:-929556700,9b9481d72932c310e7b529383e81606b:651422318,a35401d73c32c3104c958c16b1d77c27:2096493011,851441972432c3103e997c1ecd95db36:-457666345,dfd334d3687e83104975e4866bdb8847:1993475968,d5b34acfa47a03105808daf63103663a:-1339196101,d3c532a6a8b6c710d8391c5556b22256:397228824,06957662f8b6c71052835acac5bf223d:-2072526120,1d15fae226b6c7103bd9cb5c162a8abc:992558109,eda436a2cdb6c71084ea6c95dd49ecc4:-940188994,18547262e2b6c71081ccc1b71ffe740f:1311563393,469372ae3476c710143550778c0a45ef:1123954133,bcece426b4fa4710167beda164c81c9e:1815755719,f0bcace2cafa471015e9da8471764b65:-206597533,7df8ace63cba4710162ad16b1f339882:1815755719,5cd5ac6ee67a47107271e49ad14125f5:-503099204,4425e8ea567a47107a76f6c94c47c09b:1013665816,6172a0e2007a47107a48c16ae6f596c1:-503099204,e2b02c6eae3a4710d9068a8f4f9467fb:1798659627,278fb75676b247102625439a03249c7e:916253408,f90e7b1238b24710294a35f12f641286:-1099311147,d371a15626f2c310d69b679de61154e9:1855618800,a3cf59de5fb2c310afe5bc9ea98480cd:102242312,d64f159e61b2c31057c226a2b61fb230:1572085221,6e9e511e9fb2c310556542509dbf3c2b:224546755,009d5d1a49b2c310e07475559bfc78d2:1577798235,69b9191e4072c310a36be7ee851fc337:-1863546299,5ded8382c1b643102d8e6ac7d59dcb45:-760218947,0cfc83ce3776431083ae599ee5bf5c34:2014714093,c63b874a9676431074245922bad7d785:-1738107447,10facb0a617643102d6d348d45561ff3:1674467139,9d2ac3866e7643102874cd22433c2988:505848664,ea990b064176431014977b77fdcc098e:-1898026078,542987826176431029389b0bb77d1ad4:504164183,542847ce02364310c50d3b1e87025c9d:1557669103,1aa7478ead36431094ac6866ff3536f5:-1478679760,1fe6030e89364310bd59c0834a521394:-1423856108,93d64fca693643104641871ad669978e:371977163,11c64bca08364310ebbfead45f16780f:-693182104,be66878a0e364310bea090b0c118aeaf:747098351,cb364f4a383643106f994e6a06547340:1786575816,1bb5c30a3f36431038e3af60986e90d2:747098351,12158f46313643108fb1aedec13787ac:408186140,86340e8a2df6031091be1614912012bf:1573416947,14334ac6d9f60310a4092765c6a31302:-915813383,2ca0868e96b603102b9a57c3a87ec982:1573416947,e200864e39b60310030bbe677f5dfc1d:-1195672219,b165dd72eb2e4bd06650788ccde10653:1573416947,c7a3593eccea4bd022825d441ed412e5:1219658256,8fe1d1f649ea4bd01cf7a078f32468a8:1070248796,d9b119b6f2ea4bd014550e33efe55bb3:-964538160,d5b0d5f2e3ea4bd07db48740d26f890f:1767569166,9d8b41f296aa4bd09bf2f47affa28695:-2047990904,d37a4dfe206a4bd007b7d5fac66c3fb4:246610996,c3c9c57ece6a4bd06c7c742218a033a5:-1272379379,cda881ba0b6a4bd04c4c031bd54dee10:-200113458,7ad60936e26a4bd0741dfaa43d17fcaf:1505135676,dea50d32e66a4bd0c87231a05560b786:-1882444612,36d48d7ed42a4bd05fcbee122204331f:-1807839231,29f3c1bad72a4bd01e58a348cdfc6fd1:1428224684,eb6d78362ae64bd059c60d0a71184fff:-735941766,2e58f8b605a64bd0b03efcf995e6e89e:-205912144,a52870b603a64bd02e25d41737f2ffae:-1309862574,d715d6e611a6c7d0fd4e11a2de764b6d:180284541,3a34d62698a6c7d049f076632aa80465:-947126942,0aa31ea203a6c7d0780d20b84225db68:-183720586,9281daea9166c7d045bae9f5b1e5753e:-64706721,d51e4eee2e26c7d038d95095b7d63e62:-1882483506,0687316ad422c7d07d9c1384ccb0b35b:-1366602151,b837fde64322c7d07963a984e9e9cc73:1504316143,90063de61eee87d075adbbcd161dba3d:-471995295,2715f5621f22c7d09520f82d0919be33:-1222761685,f2f4fd22c322c7d025b0e373d87633e0:1641909722,7684b5ee2fee87d09c2dcf00e01e2060:-1869082,c874356afcee87d0d48e72506a83115f:1093231203,772375ea90ee87d00040103b32eab0af:-1869082,8904bc6aa4a287d06fd42fdec0380c0a:-299314118,7ef138ee33a287d0360cab4acee700c4:1734277297,1c4a8c625fea47d01bdfa93642efad39:-1477957421,ed7cff96ef2a47d02cc84e859836bafc:2142190509,482cff56922a47d09db5861150622253:-2031602566,46ea7392de2a47d07d36363c36612e60:798022476,e0d1a156cfaac3d0c177b983a13742dd:1060064032,b4b12d162faac3d0ffcb4960060c6769:-622305713,0b1069deed6ac3d0704b8b9e27214c25:563754029,40df599e076ac3d01d0db96b8311eaf9:1352237821,b22f991e556ac3d0e5a1b85302a4af39:1375877134,3566cdde2e26c3d0e47ccfe1fdc6b220:282214186,da15cdda3926c3d0b9268adf94982d80:-987484893,54a14d128626c3d0281df1bdde258c72:-968840547,f490495e34e2c3d04d563b266b971279:950753051,dbee381a17e2c3d022c29b967ebc303d:56243253,447c7c12eae2c3d00703eb89b804b31e:-1167509960,ab7b7c5e53a2c3d06d446a29add1c5ed:-1960097481,d166bc5e8e62c3d0023c1421f3caa78c:-619918165,3960501accaa83d0fa86fa196f91d536:968047006,95ef0896a3aa83d0b30f85d64cae9949:-1102145133,861e8c1229aa83d099780ad798e91679:144896483,280e88124caa83d04de8295729b5972e:-456647381,52bf1b02746283d0d0db94e440072f58:1703288620,734e62c6d6e643d04065cd9a90382995:-102992183,18da8c0a66e6cf90c1f9a37d36f4f215:-560148710,0f3a40c699e6cf90b00d7a9628724595:-1593667995,09364c4a2ea6cf903dfcceb2a55d4d9b:-424260301,94618c8a3d66cf901074b8bb01fb8696:-1189039777,499ff375b766cf901a0370ff97c648a5:-627170980,175e3b711e66cf9070e90a5c1e50dd1f:-1388902121,a66a33759126cf9033f80e9301058a32:130657453,ef1a73352b26cf9007f7309bc2d6ebe6:1238144529,5c4773f983e2cf9084020215984b796b:-1165587432,68363b75a5e2cf90220586052507d3c2:2139898637,a2657775bfe2cf906571bf78199fb9ed:1875665831,c9543f7178e2cf90e1bc323e20c05566:817047707,22933bfd60a2cf90e00710f294b0ce74:-440976559,492373bde1a2cf90029f98179e6370d9:-1593702114,422273f92ca2cf901bcca3782bf66923:1527772110,e2c07fb53ca2cf90e960731c4943a98a:-580772417</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sys_properties_691e4eee8326c7d066b576d6feaad390</name>
<payload><![CDATA[<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_properties"><sys_properties action="INSERT_OR_UPDATE"><choices/><description>JSON array for GUSS Add New File menu entries. Each item: {"label": string, "icon": string, "tableName": string}.</description><ignore_cache>true</ignore_cache><is_private>false</is_private><name>global_update_set_studio.create_artifacts</name><read_roles/><suffix/><sys_class_name>sys_properties</sys_class_name><sys_created_by>svc.spie.mcpserver</sys_created_by><sys_created_on>2026-08-10 00:34:51</sys_created_on><sys_id>691e4eee8326c7d066b576d6feaad390</sys_id><sys_mod_count>8</sys_mod_count><sys_name>global_update_set_studio.create_artifacts</sys_name><sys_package display_value="Global" source="global">global</sys_package><sys_policy/><sys_scope display_value="Global">global</sys_scope><sys_update_name>sys_properties_691e4eee8326c7d066b576d6feaad390</sys_update_name><sys_updated_by>nervoussow</sys_updated_by><sys_updated_on>2026-09-18 14:03:33</sys_updated_on><type>string</type><value>[&#13;
  {&#13;
    "label": "Business Rule",&#13;
    "icon": "⚙️",&#13;
    "tableName": "sys_script"&#13;
  },&#13;
  {&#13;
    "label": "Client Script",&#13;
    "icon": "]]>🖥<![CDATA[️",&#13;
    "tableName": "sys_script_client"&#13;
  },&#13;
  {&#13;
    "label": "Fix Script",&#13;
    "icon": "]]>🔧<![CDATA[",&#13;
    "tableName": "sys_script_fix"&#13;
  },&#13;
  {&#13;
    "label": "REST Message",&#13;
    "icon": "]]>🌐<![CDATA[",&#13;
    "tableName": "sys_rest_message"&#13;
  },&#13;
  {&#13;
    "label": "Scheduled Job",&#13;
    "icon": "⏰",&#13;
    "tableName": "sysauto_script"&#13;
  },&#13;
  {&#13;
    "label": "Script Include",&#13;
    "icon": "]]>📦<![CDATA[",&#13;
    "tableName": "sys_script_include"&#13;
  },&#13;
  {&#13;
    "label": "System Property",&#13;
    "icon": "]]>🛠<![CDATA[️",&#13;
    "tableName": "sys_properties"&#13;
  },&#13;
  {&#13;
    "label": "Table",&#13;
    "icon": "]]>🗃<![CDATA[️",&#13;
    "tableName": "sys_db_object"&#13;
  },&#13;
  {&#13;
    "label": "UI Action",&#13;
    "icon": "▶️",&#13;
    "tableName": "sys_ui_action"&#13;
  },&#13;
  {&#13;
    "label": "UI Page",&#13;
    "icon": "]]>📄<![CDATA[",&#13;
    "tableName": "sys_ui_page"&#13;
  },&#13;
  {&#13;
    "label": "UI Policy",&#13;
    "icon": "]]>📋<![CDATA[",&#13;
    "tableName": "sys_ui_policy"&#13;
  }&#13;
]</value><write_roles/></sys_properties><sys_translated_text action="delete_multiple" query="documentkey=691e4eee8326c7d066b576d6feaad390"/></record_update>]]></payload>
<payload_hash>1637938174</payload_hash>
<remote_update_set display_value="GlobalUpdateStudio-v2.2">90892690832f4bd466b576d6feaad3a8</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>nervoussow</sys_created_by>
<sys_created_on>2026-09-21 19:51:06</sys_created_on>
<sys_id>d4892690832f4bd466b576d6feaad3a8</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a0b4d46a080000001</sys_recorded_at>
<sys_updated_by>nervoussow</sys_updated_by>
<sys_updated_on>2026-09-21 19:51:06</sys_updated_on>
<table/>
<target_name>global_update_set_studio.create_artifacts</target_name>
<type>System Property</type>
<update_domain>global</update_domain>
<update_guid>503d31576d530f94001a49db7e865d25</update_guid>
<update_guid_history>503d31576d530f94001a49db7e865d25:1637938174,bf9592e1284343907282d31ecf13eb44:-699272777,c0345e6de00343908b19e874917a68ee:-1104547317,6c3278b232664bd05886aae3aa3bfa93:-1702594676,1c81b8fe6e264bd01c3837681e185bec:-1802273160,6751fcbeeb264bd085ab8a7c94c73df1:-1702594676,d92116aa7566c7d02d3787418752482e:1598237628,6cd0de2a7266c7d03b6f3cb81f3b5f69:-289024502,ca1e8eee1e26c7d081096f086c4e22f9:-910468336</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
</unload>
